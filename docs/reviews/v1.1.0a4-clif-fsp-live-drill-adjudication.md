# v1.1.0a4 — clif ⇄ fwd FSP live drill: independent Reviewer adjudication

**Date:** 2026-05-19 · **fwd:** v1.1.0a4 (running, Coston2) · **clif:** FSP
client v0.5.0 (working tree, keyless) · **Network:** Coston2 (chain 114).

The Reviewer re-derived every cryptographic check independently (recomputed
preimages from the Step-0 spec, re-ran EIP-191 recovery, re-decoded calldata
from the chain, decoded reverts) — clif self-reports were **not** taken as the
verdict (Core #14, the spawned-implementer control).

## Provisioning (operator side, this host)

- fwd redeployed v1.0.0a3 → **v1.1.0a4**, sealed master + `fwd-state` volume +
  fee-claimer policy preserved; alembic at `0006` (head); merged FSP policy
  loaded clean (`policy-load approved`, audit row 58); `check_consistency`
  (caller-binding + wallet_allowlist + **address-level segmentation with real
  DB addresses**) passed at restart.
- Two segmented wallets: `clif-fsp-signing-policy` =
  `0x0103F35A22c65b96744D6558E239f4847f2bfb04` (FSP domain),
  `clif-fsp-sender` = `0xDFC0336b32942c603274890aEf1146c71A3fC1D1` (EVM
  domain) — distinct from each other and from the claimer executor
  `0x4e9c4b5e15EaEEc28ceb09403C684Ca26e0e21e7`. Two callers `clif-fsp-sign`
  (→`fsp/clif`) and `clif-fsp-submit` (→`perm/clif-fsp-submit`).

## Test A — fee-claimer regression: **PASS**

`clif rehearse` (real builder / real RewardManager ABI / anchored selector;
never hand-authored). Independently re-derived from the Coston2 chain:

| Check | Independent re-derive |
|---|---|
| fwd accept | audit row 59 `sign-and-send approved clif-claimer-c2` |
| on-chain `from` | `0x4e9c…21e7` == fwd-custodied executor (clif keyless) |
| `to` | `0xb4f4…3454` == Coston2 RewardManager (config.py chain 114) |
| selector | `0x8e33aba5` (`claim`); recipient arg pinned `0x7c35…c294` |
| value | 0 | mined block 30704226; receipt status `0x0` (revert **expected** for empty-proof rehearsal — proof is `from`, not claim success) |

→ The FSP additions (new ABI, FSP policy blocks, migration 0006, segmentation
enforcement) **did not regress** the fee-claimer path.

## Test B1 — Leg-1 clif↔fwd (protocol-independent): **PASS**

Direct `/v1/sign-fsp-message` (caller `clif-fsp-sign`), Reviewer recompute:

- **Preimage(3077):** fwd `message_hash`
  `0x2d3852a6…f4ba58` byte-equals independent
  `keccak256(epoch_be32(3077) ‖ keccak256(0x00·32))`.
- **EIP-191 recover(3077):** `recover_message(encode_defunct(primitive=h),
  sig)` = `0x0103F35A…2bfb04` == `clif-fsp-signing-policy` DB address (v=27).
- **Step-0 frozen KAT (epoch 0, key-independent):** fwd UPTIME epoch-0 hash
  **byte-equals** the frozen Step-0 KAT
  `0xb7e97e6b4b2c7cd5fb9b51a86ad7eae441872b770b5953443024cb1e0bc6f67d`;
  `fakeVoteHash` byte-equals the Step-0 value
  `0x290decd9548b62a8d60345a988386fc84ba6bc95484008f6362f93160ef3e563`.
- Audit row 60 `fsp-sign-message approved clif-fsp-sign`; `clifwd audit
  verify` → `chain intact: 63 rows` (Core #5).

→ **GATE-1 F1 messageHash clause = CLEARED** (fwd's preimage builder is
byte-correct vs real `signing-tool@838b87f`). B1 alone answers "clif↔fwd
correct for Leg-1" with zero protocol dependency.

## Test B2(i) — Leg-2 clif↔fwd integration: **PASS (to signed-broadcast)**

`clif fsp uptime -e 3077` Leg-2 → fwd `/v1/sign-and-send` returned **422
`transaction_rejected: insufficient funds for gas, balance 0`** — *not* 403,
*not* policy-deny, *not* ABI-decode-fail. fwd authenticated `clif-fsp-submit`,
passed policy (Coston2 FSM `0xbC1F…17A1` + `signUptimeVote` +
`clif-fsp-sender` allowlist + segmentation), ABI-decoded the intent, signed
with the segmented sender, and broadcast — the **chain** rejected solely on
0-balance. Forensic-durable audit row 61 `sign-and-send error
clif-fsp-submit` (Core #19 working on the FSP submit path). The sole wall is
operator funding of `clif-fsp-sender` (runbook confound: "tx not mined =
sender unfunded"), **not a clif/fwd defect**. Read-only `eth_call` further
proves the FSM decodes our calldata and executes deep into its logic (window
guard → signer recovery → registry check).

## Test B2(ii) / GATE-1 F2 — FSP-protocol acceptance: **P3 = NO → DEFERRED**

Read-only `eth_call signUptimeVote` on the live Coston2 FSM with fwd's real
B1 signature:

- epoch 3077 (current/open): `Error(string) 'epoch not ended yet'` — window
  guard (correct ABI decode reached).
- ended epochs 3076…3000: `Error(string) 'invalid signing policy address'`.
- Contrast: corrupted-s (still recoverable) → same `'invalid signing policy
  address'`; zero-s (non-recoverable) → generic `'execution reverted'`.

The valid signature traverses the contract's full
decode→reconstruct→EIP-191-ecrecover pipeline and is rejected **only** at the
registered-voter lookup. The fresh fwd-generated signing-policy key is not
AP's registered Coston2 signing-policy voter. **P3 = NO — established
on-chain.** Expected, **not a clif/fwd defect** (runbook confound:
"Leg-2 revert signer/registration/window = P3==NO"). F2 honestly deferred
until AP's FSP signing-policy entity is registered on Coston2.

**GATE-1 named residual** (eth-account-EIP-191 ≡ web3-v4-`accounts.sign`):
the plan named "on-chain ecrecover + real-binary diff" as the arbiter. The
**on-chain ecrecover arm is now satisfied** — the live FlareSystemsManager
(the web3-v4 consumer itself) recovers fwd's eth-account EIP-191 signature
successfully (reaches the registry check past recovery; the zero-s contrast
proves that point is reached only via successful recovery). The residual is
substantially closed by the on-chain arbiter; the real-binary diff remains
belt-and-suspenders, no longer load-bearing for correctness.

## Verdict

| Item | Verdict |
|---|---|
| A — regression? | **No regression** (PASS) |
| B1 + B2(i) — clif↔fwd integration | **Correct end-to-end** (PASS; B2 to signed-broadcast, sole wall = operator funding) |
| B1c — GATE-1 **F1** | **CLEARED** (messageHash byte-equals frozen Step-0 KAT) |
| B2(ii) — GATE-1 **F2** | **P3 = NO → honestly DEFERRED** (not a clif/fwd defect) |
| GATE-1 named residual | **Substantially closed** via on-chain ecrecover arbiter |

**clif ⇄ fwd is proven correct end-to-end.** The only unproven clause is
on-chain FSP-protocol *acceptance*, blocked solely by AP not being a
registered Coston2 signing-policy voter (P3=NO) — an on-chain-identity matter,
not a clif/fwd defect.

## v1.1.0a5 decision-point (plan step 4, P3=NO branch — operator-gated)

Per the forward plan, P3=NO ⇒ F2 honestly deferred. Two options, **operator
decides with these real drill facts**:

- **(a) Defer v1.1.0a5 entirely** until a registered-voter Coston2 env exists
  (full F1 + B2(i) + F2 reconciliation shipped once).
- **(b) Ship a narrowed v1.1.0a5** now — bounded-reconciliation per D17,
  reconciling ONLY what F1 + B2(i) proved (the reconstructed EIP-191 digest
  is cryptographically real, on-chain-recoverable by the real FSM, and
  broadcastable), explicitly marking the on-chain-FSM-acceptance clause
  *deferred* (D17 constraint (i): only invariant text the byte-verified code
  makes true).
