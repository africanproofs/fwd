# v1.1.0a72 — onboarding output in plain operational English

**Type:** presentation-only change to `install/onboard` (operator-authored, Reviewer adversarially validated). No custody / signing / nonce / policy / clif-env / daemon / money-path behaviour change.
**Date:** 2026-06-04.

## Why

The onboarding output was technically correct but overloaded an experienced Flare/FSP operator with ABI/contract-parameter jargon (`_recipient`, `_rewardOwner`, "RewardManager.claim called by executor", "FlareSystemsManager.signUptimeVote / signRewards") at exactly the moments they need clear action. The operator rewrote the presentation text into plain operational English while preserving every safety gate and every action.

## What changed (`install/onboard` only — +43/−45, all inside output-string arguments)

- Recipient / identity prompts: contract-parameter phrasing → plain "reward recipient" / "reward owner / data-provider identity".
- Guided intro + capability summary: "fwd claims rewards for your identity and sends them to your recipient" / "fwd signs uptime/reward messages with your registered signing key"; the "what onboarding writes" framing states plainly that it writes policy, stores keys in fwd, mints caller tokens, seeds nonces from chain, writes clif env — and that clif never receives private keys and fwd signs only.
- Step / wallet-role / clif-env descriptions reworded to plain English ("generated and sealed in fwd", "imported and sealed in fwd", "already present"; "the wallet that pays gas and submits FSP messages"; env files "contain wallet names and caller tokens only, no private keys, mode 0600").
- Final cutover/authorization block kept the exact method names the operator must act on — `ClaimSetupManager.setClaimExecutors`, `setAllowedClaimRecipients`, register the FSP signer as the signing-policy/voter address — plus the gas-wallet, `clif`-wrapper-not-raw-compose, `RewardClaimed`, and `clifwd audit verify` lines.

## Validation (adversarial binding-pass)

Confirmed presentation-only: every changed line is inside a `printf`/`note`/`warn`/`guided`/`step`/`fsection`/`ask_tty` string; no control flow, variable, command, wallet name, policy path, nonce reconciliation, caller-token handling, clif-env key, or daemon/profile change; `git status` shows only `install/onboard`. `sh -n` + `dash -n` pass. Every safety gate (recipient/identity validation, key-paste + address confirm, deferred-import/token-rotation/nonce-drift/cutover/no-clif-layer warnings, the CUTOVER and on-chain-authorization blocks, the `clif`-wrapper warning, `RewardClaimed` + `clifwd audit verify`, the unresolved-drift `exit 1` and complete terminators) prints **unconditionally** in both compact and guided modes — none is behind `guided()` (the only `guided()`-gated lines are explanatory framing, already gated pre-change). Accuracy of the simplified phrasing verified against the code. Verdict: SAFE. Two MINOR optional polish notes (cutover-authorization hedge; a dropped gas-wallet rationale) recorded in the review and left to the operator — not applied.

## Files

`install/onboard`, version files.
