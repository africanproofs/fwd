# v1.0.0a3 — sealed-master Core #14 gate MET live (Reviewer-only)

**Shipped**: 2026-05-18
**Author**: Opus 4.7 Reviewer. No Sonnet; no `src/` change beyond `version.py`.
**Predecessor**: v1.0.0a2 (`a5c90d2`, verification runbook published).

## What this records

The v1.0.0a1 sealed-master custody swap's **Core invariant #14 live
gate is MET on Coston2** (2026-05-18). Same precedent as v0.3.1
(Phase 3 sign-and-send proof), v0.4.5/v0.4.6 (Phase 5/6 proofs), v0.5.3
(Phase 7 GA proof): the runbook was published at v1.0.0a2 and the
**verification-met addendum** is appended to
`docs/history/1.0.0a1-vault-to-sealed-master.md` once the operator runs
it. Full evidence lives in that addendum; the headline:

- Stack recreated from the v1.0.0a1 tree — `fwd` + `fwd-litestream`
  only, **no `vault` / `vault-snapshot` / `fwd-internal`** anywhere.
- `/healthz` → `master:"ok"` (field is `master`, not the retired
  `vault` — the v1.0.0a1 health.py rewrite is live).
- Wallet `sealed-verify` `0xf49C60A324553956D197C4C5Bc15B57b0Adb4231`,
  privkey `seal:v1:…` (no `vault:`, no plaintext); `vault_master_key`
  column `local:v1`.
- `POST /v1/sign-and-send` (caller-gated, default-deny enforced) → tx
  `0xc222bf62…5ac5e6` mined block **30662555**, `status=0x1`,
  `type=0x2`, on-chain **`from` = `$ADDR` (secp256k1 custody recovery
  through the sealed-master path — no Vault)**.
- `clifwd audit verify` → **chain intact: 37 rows, exit 0**.

A matching on-chain `from` is impossible unless `SealedMaster.decrypt()`
AES-256-GCM-decrypted the exact privkey from its `seal:v1:` ciphertext
under the mode-0600 host-file master. The sealed-master custody chain
is proven end-to-end with zero Vault in the path.

## Drill value (Core #14 earned its keep again)

The live run surfaced a **runbook-shape defect**, not a code defect.
The v1.0.0a2 runbook's premise — an empty-`data` value send exercises
the custody path — is wrong: Core invariant #3 ("sign intent, never
opaque bytes") correctly **denies** undecodeable calldata at policy
step 3 (`policy_denied step=3: calldata too short`). There is no
policy-passable bare value transfer through `/v1/sign-and-send` by
design. Per the v0.5.1 precedent (drill surfaces doc/substrate drift →
fix folded into the evidence ship), the runbook was corrected in place
to the proven `phase-7-verification.md` shape — a self-addressed ERC20
`transfer(address,uint256)` with `amount=0`, the EOA target accepted
under phase-7's same operator-gated premise (the gate verifies fwd's
decode→policy→sealed-master-decrypt→sign→broadcast→custody path, not
token semantics). Edits: Step 2 policy `methods`, the Step 2 note,
Step 5 calldata-build curl, pass/fail row 5 (`201`→`200`). The custody
property under test is unchanged — Core invariant #18 (doctrine/code
no-drift): the runbook now matches the code's real default-deny
behavior.

## Gates

No `src/` change beyond the `version.py` string; doc + runbook surgery
only. `pytest` / ruff / format / mypy --strict / layer-boundary /
version-consistency re-run green; 426 passed + 0 skipped unchanged.
Version 1.0.0a2 → 1.0.0a3.

## Next

**Phase 8b** (`ftso-fee-claimer` cutover; the first deleted
`.env PRIVATE_KEY=` line; v1.0.0 — lifts the doctrine-ship freeze) is
**unblocked**, gated by Core invariant #15 (operator approval). The
`sealed-verify` wallet/caller + the minimal gitignored
`config/policy.yaml` are the drill's live state; the operator's
pre-v1.0.0a1 phase-7 policy is preserved at
`config/policy.yaml.pre-v1.0.0a1.bak`.

## Commit

Single terse conventional-commit line, NO trailer, operator sole
author (root constitution NON-NEGOTIABLE). Committed local-only; push
remains blocked by the GitLab account block; `origin/main` at
`f1ee3f1`.
