# v1.1.0a27 — QA reconciliation stragglers

**Type:** sanitization reconciliation (continuation of a22/D21; the two findings a26 left open).
**Date:** 2026-05-28.
**Authoring:** Reviewer (Opus); operator-gated (the same QA round that drove a26).

## Why

A second read-only QA pass (run against a26's parent, `737c264`) re-listed the a26 findings — all already closed in a26 — plus two that a26's own sweep had not landed at the exact spot, because a26's archaeology grep pattern didn't match their wording:

1. `docs/architecture.md:231` — "**Every failure** writes an audit row …" reads absolutely. Pre-handler rejects (auth failures, malformed-header 400s, read-only/health GETs) are not in the sign path and write no audit row, so the absolute wording overclaims coverage the same way the a26-tightened Core #5 / README / threat-model lines used to.
2. `docs/dependencies.md` — the "Build, dev, and test tooling" table listed pytest/ruff/mypy but omitted `types-pyyaml` (a real `tool.poetry.group.dev` entry, the PyYAML stubs `mypy --strict` needs). a26 fixed the *runtime* table (re-added `eth-abi` + `cryptography`) but not this dev table.

## What changed

- `docs/architecture.md` — the post-failure-table sentence now scopes to "every denied or errored operation **in this sign path**", and explicitly notes pre-handler rejects write no audit row. Consistent with the a26 audit-coverage tightening and Core invariant #5.
- `docs/dependencies.md` — added the `types-pyyaml` (`^6.0`) row to the dev-tooling table.

## Not changed
The rest of the second QA's list was already closed in a26 (runbooks quarantined; status enum; runtime deps; audit overclaims in Core #5/README/threat-model; "every theft attempt visible"; §Scope versionless; `.env.example`; config/abis dangling ref; threat-model host-hardening/revision-history; architecture layer-boundary; compose nonce-CLI comment; `sign-and-send` enum values removed from code). Code-internal Vault/`sign-and-send` symbol naming stays the deferred operator-gated refactor.

## Verification
- `pytest tests/unit/test_version_consistency.py` — passes (a26→a27 bump consistent).
- Doc-only change otherwise; ruff/mypy/full suite unaffected (476/1 as of a26).
- Archaeology grep over the current-state surface — clean (only live `vault_*` symbols + governance enactment markers remain).
