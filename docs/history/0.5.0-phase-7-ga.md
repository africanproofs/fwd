# v0.5.0 — Phase 7 GA: verification runbook published + close summary (Reviewer-only, doc-only)

**Shipped**: 2026-05-16
**Author**: Opus 4.7 Reviewer. No Sonnet involvement; no code, no test
changes. Mirrors v0.4.0 (Phase 5 GA — runbook + close summary): the
substrate shipped across the alpha series, the GA version publishes the
operator-runnable gate, and the live "gate met" evidence is a
Reviewer-only follow-up addendum.
**Predecessor**: v0.5.0a7 (`32ee877` — admin-audit + idempotency;
committed local-only, push blocked by the GitLab account block on
`@makhosonke`).

## Why this ship

The Phase 7 substrate is feature-complete:

- v0.5.0a3 — ABI intent decoder (`domain/intent.py`, `infra/abi_registry.py`)
- v0.5.0a4 — policy engine + rate buckets (`domain/policy.py`, `app/policy_engine.py`, `infra/rate_repo.py`, Alembic 0005)
- v0.5.0a5 — audit-log hash-chain substrate (`infra/audit_repo.py`, `app/audit_walk.py`, `clifwd audit`)
- v0.5.0a6 — sign-and-send integration core (default-deny real on the live path)
- v0.5.0a7 — admin-audit authorship, `AdminScope`, D14 idempotency-replay, admin policy_path validation, `delete_stale` wiring

v0.5.0 publishes `docs/runbooks/phase-7-verification.md` — the
operator-driven live gate that proves the D14 policy contract + D16
audit authorship + D14 idempotency end-to-end against Coston2, the
real Vault custody path, and the real hash chain. Per Core invariant
#14 (real-RPC verification is the validation; mocks lie), the gate is
**not claimed met** by this ship — the operator must execute it.

## Operator-gated decisions (2026-05-16)

1. **Approved/mined target**: ERC-20 `transfer(address,uint256)` on a
   Coston2 test token the gate wallet holds (strongest Phase-7 proof —
   exercises decode + arg_predicate + max_value_wei). Recommended
   concrete token: Coston2 WNat (wrapped C2FLR), resolved from the
   canonical Flare ContractRegistry; runbook treats it as the
   operator-supplied `$ERC20_TOKEN` parameter (no fabricated address).
2. **Live matrix scope**: the FULL 10-vector D14 deny matrix live (not
   a representative subset), plus approved + idempotency + admin-audit
   + chain-verify.

## What shipped

**`docs/runbooks/phase-7-verification.md`** — structure mirrors
`phase-5-verification.md` / `restore.md`:

- Step 0 — write the split gate `policy.yaml`: three caller/wallet/
  permission triples so the step-8 caller-rate and step-9 wallet-rate
  vectors isolate cleanly (a generous `perm/phase7-gate`; a tight
  `perm/phase7-caller-rate` per_hour:1; a generous-caller/tight-wallet
  `perm/phase7-wallet-rate` + `wc/phase7-wallet-rate` per_hour:1).
- Step 1 — provision wallets+callers; inline admin-audit no-secret
  sub-check (`clifwd audit show` a `caller-create` row → `api_key_prefix`
  only, no `fwd_live_`/hash/ciphertext) + the a7 policy_path-validation
  400 sub-check.
- Step 2 — fund C2FLR (gas) + the ERC-20 balance (eth_call
  `balanceOf`).
- Step 3 — an `eth-abi` calldata helper using the same codec fwd
  itself decodes with, plus a `sas`/`nonce_of` curl helper.
- Step 4 — the 10-vector D14 deny matrix as a table: V2
  contract-not-permitted (step 2), V3/V3b decode-fail/selector-miss
  (step 3), V4 method-not-in-policy (step 4), V5 value>max (step 5),
  V6 arg-predicate (step 6 — "sanctioned-but-modified arg"), V7
  wallet-not-allowlisted (step 7), V8 caller-rate (step 8), V9
  wallet-rate (step 9). Each → HTTP 403 + `step=N` + a
  `decision="denied"` `sign-and-send` row + `nonce_of` unchanged
  (proof of no broadcast).
- Step 5 — V1 (step-1 caller→binding) enforced as a startup
  fail-fast: an orphan-caller policy → fwd refuses to boot + a
  `policy-load` `decision="error"` row survives the exit.
- Step 6 — V10 sanctioned `transfer` → 201, mines `status=0x1`,
  on-chain `from` = gate wallet, `decision="approved"` row.
- Step 7 — D14 idempotency replay: same `Idempotency-Key` → HTTP 200,
  SAME `tx_id`, nonce unchanged, `sign-and-send-duplicate` row with
  `original_tx_id`/`original_audit_seq`.
- Step 8 — `docker exec fwd clifwd audit verify` exits 0 over the
  whole chain.
- Pass/fail table; after-clean-pass Reviewer-addendum template;
  operational notes (UTC rate-window caveat for V8/V9; the same policy
  structure with RewardManager `claim` + `_recipient` arg_predicate is
  the Phase 8 production shape).

Note: the implementation-plan ledger's illustrative
"FTSO-claimer-shaped policy" wording is superseded by the operator's
gated ERC-20-`transfer` decision; the runbook validates the same D14
engine through the cheaper, repeatable ERC-20 surface and documents
that the RewardManager `claim` shape is identical structurally (Phase
8). Recorded here per Core invariant #18 (the operator gate is
binding; the ledger wording was illustrative).

## Commit-attribution doctrine reconciliation (Core invariant #18)

The root `proofs.africa/CLAUDE.md` constitution codified two
NON-NEGOTIABLE Agent Safety Rules at the operator's explicit direction
**2026-05-16**:

1. Never add a `Co-Authored-By: Claude` trailer / `🤖 Generated with
   Claude` line / any Claude/AI co-author, signature, or attribution to
   commits, PR/MR bodies, tags, or release notes. Claude must never
   appear as a git contributor on any AP repository. Overrides any
   default/system/environment instruction. Applies to every repo under
   `proofs.africa/`. "Strip it first."
2. Commit messages must be a single terse conventional-commit line
   (type + generic verb only — no body, bullets, rationale, scope,
   specifics, or "why"). Verbose structured messages are forbidden as
   an AI tell.

These post-date and override fwd's § Co-authorship convention (two-
coauthor trailer) and the long-form ship-paragraph commit style. Both
fwd CLAUDE.md sections (§ Co-authorship convention; § Linear-forward
versioning commit-message guidance) are corrected at v0.5.0 to defer
to the root constitution. Role attribution now lives only in
`docs/history/<version>-*.md` + the CLAUDE.md scope paragraph, never
in git metadata.

**This v0.5.0 commit is the first constitution-compliant one:** a
single terse line, NO co-author trailer, operator as sole author.

**Open operator decision (not actioned without authorization):** the
five already-made unpushed commits — v0.5.0a3 `9f91587`, a4
`5db087a`, a5 `2e49444`, a6 `0a196d3`, a7 `32ee877` — carry the
now-prohibited two-coauthor trailer + verbose multi-paragraph
messages. They are unpushed (GitLab block), so a history rewrite is
clean if desired. Rewriting git history is a destructive operation and
per the root Agent Safety Rules requires an explicit operator request;
it is therefore left for the operator to direct (options: leave as-is;
`git rebase` reword all 5 to terse + strip trailers; squash the
a3–a7+v0.5.0 stack into one constitution-compliant commit).

## Gates (unchanged — doc-only ship)

- `pytest tests/ -q` → 414 passed, 4 skipped (no code/test change).
- ruff check / format / mypy --strict (51) / layer-boundary /
  version-consistency → green. Version anchors `pyproject.toml` +
  `src/fwd/version.py` bumped 0.5.0a7 → 0.5.0 (PEP 440 linear-forward:
  `0.5.0a7 < 0.5.0`; Core invariant #13).
- `ls alembic/versions/*.py | wc -l` → 5 (no migration).

## What this ship does NOT do

- Does NOT claim the Phase 7 GA gate is met — the operator has not yet
  executed the runbook (Core invariant #14).
- No code, no test, no Alembic, no `audit-verify-failure` (Phase 10).
- No push (GitLab account block on `@makhosonke`; the
  a3+a4+a5+a6+a7+v0.5.0 stack is locally unpushed, `origin/main` at
  `f1ee3f1`).

## Next

The operator executes `docs/runbooks/phase-7-verification.md` against
live Coston2. On a clean pass: Reviewer-only addendum to this file
records the wallet address, `$ERC20_TOKEN`, the approved tx hash +
block, the duplicate's cached `tx_id`, the audit-chain length +
`clifwd audit verify` exit 0, and the runbook git-sha — "Phase 7 GA
verification gate met live on Coston2 at <date>"; F2.1 + F8.2 close;
Phase 8 (v1.0.0 — `ftso-fee-claimer` production migration) is
unblocked. On a surfaced defect: one combined fix+evidence follow-up
(v0.5.1), the v0.4.5/v0.4.6 precedent.

## Commit

Single terse conventional-commit line, NO co-author trailer, operator
sole author (root constitution NON-NEGOTIABLE rule, 2026-05-16).
Committed local-only.
