# v1.1.0a24 — dead code/config removed

**Ship type:** removal of code/config no longer part of the project (operator directive
"sanitize away from the docs AND the project"). Suite-verified.

## Removed

- **Retired Vault-snapshot sidecar:** `config/vault-snapshot/Dockerfile` + `scripts/
  vault-snapshot.sh` + `scripts/vault-snapshot-entrypoint.sh` (not referenced by
  `docker-compose.yml` or CI).
- **Stale operational runbooks** (for retired designs): `docs/runbooks/vault-init.md`,
  `restore.md`, `sign-and-send-verification.md`, `phase-5-verification.md`,
  `phase-7-verification.md`. (Kept: `v1.0.0a1-sealed-master-verification.md`,
  `ci-integration.md`.)
- **Vestigial `replaced` tx status:** dropped from `_VALID_STATUSES`
  (`transaction_repo.py`) — nothing has written it since replacements keep the tx
  `submitted`; reworded the `sign-replacement` denied-path audit reason
  `…->replaced` → `…->replacement`.
- **Orphaned bytecode:** `src/fwd/app/__pycache__/{receipt_watcher,nonce_reconcile,
  wallet_balances}.cpython-312.pyc` (the `.py` were deleted long ago).

## Verification

`ruff check` clean, `mypy --strict src/` clean (56 files), full suite **476 passed /
1 skipped**, `docker compose config` valid (vault-snapshot removal does not affect the
compose stack — it was unreferenced), no `"replaced"` literal left in `src/`.

## Flagged follow-up (a25 — operator-gated, NOT done here)

Vault naming survives in CODE internals and is a deeper refactor:
- **Symbol renames** — the `EnvelopeSigner` class, the `self._vault` attributes, and
  the `VaultUnavailableError` exception still carry the old name. Renames touch many
  files + tests.
- **The `vault_master_key` SQLite column** (holds `local:v1`) — a rename needs an
  Alembic migration.
- **The `vault_unavailable` (503) error code** — renaming changes an API contract;
  consumers (`fwd-client`, clif) must move in lockstep.
- **Code comments/docstrings** referencing Vault (`health.py`, `sealed_master.py`,
  `envelope_signer.py`, `settings.py`, `db.py`, `dependencies.py`, layer `__init__.py`
  docstrings) + the `ci-integration.md` runbook's stale Vault content.

These are bundled as one coherent a25 refactor (migration + API-contract care) for
operator gating, not rushed. The current-state DOCS are already fully sanitized
(a22 CLAUDE.md, a23 README/architecture/threat-model/dependencies).
