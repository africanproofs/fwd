# v1.0.0 — Phase 8b fwd-side; the doctrine-ship freeze LIFTED

Reviewer-driven; no Sonnet. The terminal ship of the freeze era. Two
independent clean binding passes + the bounded fold-ins; the freeze
re-scope's release condition is met.

## The two clean binding passes (the freeze-lift trigger)

1. **Live Core-#14 calldata binding pass — CLEAN.** clif's *real*
   `RewardManager.claim` calldata, built by clif's real
   `build_claim_calldata`, signed through fwd's sealed-master path, mined
   on Coston2 at blocks **30669188** (nonce 0) and **30669250**
   (nonce 1); on-chain `from` = the fwd executor wallet
   `0x4e9c4b5e15EaEEc28ceb09403C684Ca26e0e21e7`. Independently
   re-decoded by the Reviewer through fwd's real `AbiRegistry.load`/
   `decode_intent`: selector `0x8e33aba5`, pinned `_recipient`
   `0x7c3579aB…cc294`, B1 projection holds. Reverting on-chain is
   expected — the proof is the recovered `from` (the v1.0.0a3
   precedent). Custody chain proven end-to-end with zero key in clif.

2. **clif STOP-SHIP binding pass — CLEAN.** The fourth independent audit
   (`docs/reviews/v1.0.0-system-independent-audit{,-report}.md`,
   untracked decision inputs) returned GO-WITH-CONDITIONS: fix Findings
   1–3, re-test, no further audit. The sibling shipped clif `1357ebf`
   (v0.3.0, 55 tests). Two independent Explore agents verified the
   *actual pushed commit* against fwd's *authoritative real-code seam
   contract* (not the prose). Adjudicated CLEAN: #1 `.env`-source
   key-name scan (values never read); #2 retry discriminator
   (`retry=None` byte-identical to legacy — in-flight dedup preserved;
   fresh key only on an explicit operator token; never auto-randomized;
   correctly handles fwd's status-blind replay); #3 transport→retryable
   in sign/status/health + `_TERMINAL {400,401,403,404,422}` /
   `_RETRYABLE {502,503}` exactly matching fwd, unmapped→fail-closed→
   `submit_claims`-caught (clif `auto` never crashes — total). Rehearse
   walled off; zero fwd changes; keyless intact; operator sole author,
   no trailer. **No fwd change required** (fwd correct-as-designed for
   #2/#3, confirmed against real code).

## What this ship contains (bounded)

The freeze-era doctrine surface was split into two ships to keep each
bounded (the fourth audit's "keep v1.0.0 bounded" + the operator-named
CLAUDE.md cleanup):

- **Ship N (`d4ca777`, prior):** the CLAUDE.md corpus-bloat remedy — the
  52-paragraph § Scope log relocated verbatim to `SHIP-LOG.md` +
  `README.md` index (byte-identical, proven); the CLAUDE.md-resident
  doctrine-honesty (Identity/Core-#16 sealed-master, Core-#17
  flag-not-rewrite of the unverified DR mechanic, the v1.0.0-rollup
  `setClaimExecutors`); the durable per-ship-doctrine-surface convention
  that kills the bloat generator + the recurring §-Scope-drift class.

- **Ship N+1 (this, v1.0.0):** code/config/non-CLAUDE.md-doctrine/
  version. Verified correctness: the broadcast-classification fix
  (`RpcError`→release nonce+rate+terminal `TransactionRejected`/422;
  `RpcUnavailable` unchanged) + regression — **live-proven** (the funded
  rehearsal re-run mined at nonce 0; the wedge recurs without it); the
  `SealedMaster` O_NOFOLLOW/fstat-fd-bound hardening + tests.
  Active-setup honesty: `.env.example` Vault→`FWD_MASTER_KEY_FILE` +
  Litestream-only backup. `decisions.md` D14 status-blind-replay
  honesty clause. The `setClaimRecipient`→`setClaimExecutors`
  correction in the Phase-8b-cutover-context non-CLAUDE.md lines
  (`implementation-plan.md`, `dependencies.md`, `decisions.md`
  D-records — incl. closing the `decisions.md:137` "inside Vault"
  forward-instruction mechanical-mirror tail named at v1.0.0a1).
  Root `proofs.africa/CLAUDE.md:42` corrected on-disk (the org
  constitution is not under git — no commit). Version 1.0.0a3→1.0.0.

**No-revisionism preserved:** the DR/asset-class `setClaimRecipient`
occurrence at `decisions.md:35` is left verbatim — it is covered
doctrine-wide by the Ship-N Core-#17 flag (the DR mechanic is
Reviewer-unverified; rewriting it would inject an unverified mechanic
into security doctrine). `docs/history/*` and `docs/reviews/*` untouched.

## Face-C — named deferral (NOT v1.0.0 code)

A distinct nonce-lifecycle gap surfaced reasoning through the broadcast
fix: a tx the node *accepts* (hash, no error) that never mines → the
receipt watcher exhausts retries, marks it `failed`, and the reserved
nonce is orphaned forever (`nonce_reconcile` logs the drift, never heals
it). fwd has no nonce-reset surface by design. Explicitly deferred to
Phase 9/10 nonce-lifecycle work — Core invariant #18 (a named,
discoverable deferral in `decisions.md` D14, never silent drift; the
v0.4.5 precedent: fix the drill-surfaced bug, name-and-defer the
deeper follow-up).

## The freeze, lifted

The doctrine-ship freeze (operator-approved, re-scoped 2026-05-18)
releases on fwd v1.0.0 shipped on a live Core-#14 proof. Met. The
control **survives as a STANDING rule**: a fwd ship is permitted ONLY
for (i) a named consumer-driven custody capability, (ii) committing
already-verified correctness, or (iii) Phase-10 hardening — never an
audit / reconciliation / scope-ceremony ship, absent an operator-named
concrete consumer need. Forward path: Phase 9 (the structured
protocol-message-signing endpoint — the real remaining fwd
functionality) → Phase 10. The consumer `.env PRIVATE_KEY=` deletion
is clif's decoupled milestone (its STOP-SHIP audit conditions are
cleared; the cutover is operator + the sibling agent).

Single terse conventional commit, no trailer, operator sole author;
local + pushed `github/main` (private `github.com/africanproofs/fwd`;
the GitLab `@makhosonke` block superseded).
