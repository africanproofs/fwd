# fwd ship history — index

Per-version deep ship records. The full chronological narrative v0.1.0→v1.0.0a3 (verbatim, relocated from the old CLAUDE.md § Scope) is in `SHIP-LOG.md`. Both are append-only honest history — Core invariant #18, no revisionism. A new version adds its deep file here + one line below (see fwd `CLAUDE.md` § Linear-forward versioning).

- **v0.1.0** — documentation-only inception (no deep file; see `SHIP-LOG.md`)
- **v0.1.1** — pre phase 1 fixes — `0.1.1-pre-phase-1-fixes.md`
- **v0.1.2** — vault transit pivot — `0.1.2-vault-transit-pivot.md`
- **v0.2.0** — spike coston2 — `0.2.0-spike-coston2.md`
- **v0.2.1** — pre phase 2 fixes — `0.2.1-pre-phase-2-fixes.md`
- **v0.2.2** — wallet create import — `0.2.2-wallet-create-import.md`
- **v0.2.3** — clifwd rename — `0.2.3-clifwd-rename.md`
- **v0.3.0a1** — vault deployment — `0.3.0a1-vault-deployment.md`
- **v0.3.0a2** — vault addr fix — `0.3.0a2-vault-addr-fix.md`
- **v0.3.0a3** — dev shortcut doctrine — `0.3.0a3-dev-shortcut-doctrine.md`
- **v0.3.0a4** — token lifecycle doctrine — `0.3.0a4-token-lifecycle-doctrine.md`
- **v0.3.0a5** — handoff demarcation doctrine — `0.3.0a5-handoff-demarcation-doctrine.md`
- **v0.3.0a6** — wallet provisioning — `0.3.0a6-wallet-provisioning.md`
- **v0.3.0** — alpha phase 2 scaffold — `0.3.0-alpha-phase-2-scaffold.md`
- **v0.3.0** — phase 3c sign and send — `0.3.0-phase-3c-sign-and-send.md`
- **v0.3.2** — audit corrections — `0.3.2-audit-corrections.md`
- **v0.3.3** — pre phase 4 doctrine — `0.3.3-pre-phase-4-doctrine.md`
- **v0.4.0a1** — caller auth — `0.4.0a1-caller-auth.md`
- **v0.4.0a2** — audit corrections — `0.4.0a2-audit-corrections.md`
- **v0.4.0a3** — state schema and tx persistence — `0.4.0a3-state-schema-and-tx-persistence.md`
- **v0.4.0a4** — prompt wrapper doctrine — `0.4.0a4-prompt-wrapper-doctrine.md`
- **v0.4.0a5** — nonce manager — `0.4.0a5-nonce-manager.md`
- **v0.4.0a6** — receipt watcher — `0.4.0a6-receipt-watcher.md`
- **v0.4.0a7** — admin wallet inventory and cli tests — `0.4.0a7-admin-wallet-inventory-and-cli-tests.md`
- **v0.4.0** — phase 5 ga runbook — `0.4.0-phase-5-ga-runbook.md`
- **v0.4.1** — phase6 litestream s3 and restore runbook — `0.4.1-phase6-litestream-s3-and-restore-runbook.md`
- **v0.4.2** — phase6 vault snapshot and restore — `0.4.2-phase6-vault-snapshot-and-restore.md`
- **v0.4.3** — backup substrate reversion — `0.4.3-backup-substrate-reversion.md`
- **v0.4.4** — f6.2 ci integration — `0.4.4-f6.2-ci-integration.md`
- **v0.4.5** — phase 5 verification met and concurrency fix — `0.4.5-phase-5-verification-met-and-concurrency-fix.md`
- **v0.4.6** — phase 6 drill drift fixes — `0.4.6-phase-6-drill-drift-fixes.md`
- **v0.5.0a1** — phase 7 doctrine — `0.5.0a1-phase-7-doctrine.md`
- **v0.5.0a2** — phase 7 doctrine self review — `0.5.0a2-phase-7-doctrine-self-review.md`
- **v0.5.0a3** — abi intent decoder — `0.5.0a3-abi-intent-decoder.md`
- **v0.5.0a4** — policy engine — `0.5.0a4-policy-engine.md`
- **v0.5.0a5** — audit log — `0.5.0a5-audit-log.md`
- **v0.5.0a6** — sign and send integration — `0.5.0a6-sign-and-send-integration.md`
- **v0.5.0a7** — admin audit idempotency — `0.5.0a7-admin-audit-idempotency.md`
- **v0.5.0** — phase 7 ga — `0.5.0-phase-7-ga.md`
- **v0.5.1** — phase 7 ga substrate and partial evidence — `0.5.1-phase-7-ga-substrate-and-partial-evidence.md`
- **v0.5.2** — phase 7 ga deny audit fix — `0.5.2-phase-7-ga-deny-audit-fix.md`
- **v0.5.3** — phase 7 ga verification met — `0.5.3-phase-7-ga-verification-met.md`
- **v0.5.4** — core invariant 19 forensic row durability — `0.5.4-core-invariant-19-forensic-row-durability.md`
- **v0.5.5** — audit reconciliation — `0.5.5-audit-reconciliation.md`
- **v0.5.6** — hygiene cleanup — `0.5.6-hygiene-cleanup.md`
- **v1.0.0a1** — vault to sealed master — `1.0.0a1-vault-to-sealed-master.md`
- **v1.0.0a2** — sealed master verification runbook — `1.0.0a2-sealed-master-verification-runbook.md`
- **v1.0.0a3** — sealed master verification met — `1.0.0a3-sealed-master-verification-met.md`
- **v1.0.0** — phase 8b fwd-side; doctrine-ship freeze lifted — `1.0.0-phase-8b-fwd-side.md`
- **v1.1.0a1** — phase 9a-i fsp signer primitive — `1.1.0a1-phase-9a-i-fsp-signer-primitive.md`
- **v1.1.0a2** — phase 9a-ii fsp signing endpoint — `1.1.0a2-phase-9a-ii-fsp-endpoint.md`
- **v1.1.0a3** — constitutional amendment: bounded non-feature ship-types — `1.1.0a3-constitutional-amendment-shiptypes.md`
- **v1.1.0a4** — constitutional amendment: hand-off embeds the file in the message — `1.1.0a4-handoff-embed-amendment.md`
- **v1.1.0a5** — bounded post-verification reconciliation: FSP doctrine ⇄ byte-verified code (GATE-1 F1 cleared; F2 honestly deferred — P3=NO) — `1.1.0a5-bounded-reconciliation-fsp.md`
- **v1.1.0a6** — bounded key-domain segmentation carve-out: FSP signer self-submit (default-off opt-in; constrained FSM-only-zero-value; invariant *text* reconciliation deferred to Ship E post-GATE-2) — `1.1.0a6-segmentation-carveout-fsp-self-submit.md`
- **v1.1.0a7** — `wallets balances` admin read-only feature: per-(wallet, chain) gas balance for fwd-custodied transacting wallets, derived from policy.permissions[*].contracts via a small contract→chain table — `1.1.0a7-wallets-balances.md`
- **v1.1.0a8** — admin `nonce-init`: RPC-free `POST /v1/admin/nonce-init` + `clifwd nonce init` to seed a (wallet, chain) next_nonce; `nonce_repo` on `AdminScope`. First (additive) ship of the zero-egress initiative — `1.1.0a8-nonce-init.md`
- **v1.1.0a9** — zero-egress excision / sign-only: delete `rpc.py`+receipt-watcher+nonce-reconcile+wallet-balances, `sign_and_send`→`sign_transaction` (client gas/fee + `FWD_MAX_GAS`/`FWD_MAX_FEE_PER_GAS` caps, local tx-hash, no broadcast), `/v1/sign-and-send`→`/v1/sign-transaction`, `rpc` field off `/healthz`. fwd makes zero network calls — `1.1.0a9-zero-egress-excision.md`
- **v1.1.0a10** — tx lifecycle report-back: client→fwd `POST /v1/transactions/{id}/broadcast-result` + `/receipt` so fwd tracks chain truth; `reverted` status; multi-client trust boundary (caller-ownership 404, tx_hash validation, legal-transition guards) with commit-before-raise refusals — `1.1.0a10-tx-report-back.md`
- **v1.1.0a11** — network lockdown: `docker-compose.yml` `fwd-callers` → `internal: true` (fwd container has no internet route — verified live); clients dual-homed. Network-level enforcement of zero egress — `1.1.0a11-network-lockdown.md`
- **v1.1.0a12** — live drill + checksum/nonce-release fix: end-to-end run (fwd signs zero-egress → client broadcasts to real Coston2 → report-back) caught a latent bug (non-checksummed `to` → eth_account TypeError → 500 + wedged nonce). Fix: checksum `to`; catch TypeError so any sign-build error releases the nonce. + demo client/policy in `examples/` — `1.1.0a12-live-drill-checksum-fix.md`
- **v1.1.0a13** — replacement + reclaim + admin nonce-sync: client-triggered `sign-replacement` (same-intent, same-nonce, ×1.125 bump, ≤5); `transaction_attempts` + `reserved_at` (migration 0007); admin `nonce-sync` (bounded monotonic) + `nonce/holes` (orphan surfacing); reclaim mechanic = same-intent replacement + operator alarm, never cross-intent reissue — `1.1.0a13-replacement-reclaim-nonce-sync.md`
- **v1.1.0a14** — D20 zero-egress constitutional amendment (operator-authorized): reconciles Core #3/#4/#11/#14 + "What FWD IS NOT" + threat-model A4 + a `decisions.md` D20 record to the shipped a9–a13 code. Doctrine-only — `1.1.0a14-D20-zero-egress-amendment.md`
- **v1.1.0a15** — no-host-port correction: the a14 prod cutover proved `internal: true` removes host port publishing (the a11 "published ports still work" claim was false). Removed the dead `ports:` line; admin now via `docker exec fwd clifwd`; clients reach `fwd:8080` intra-network. Compose/doctrine only — `1.1.0a15-no-host-port-correction.md`
- **v1.1.0a16** — README brought current: the public `README.md` was frozen at v0.3.0-alpha (Vault, Scaleway, `/v1/sign-and-send` as a future item, "no public exposure", dead `operator-runbook.md` link) while code reached a15. Full rewrite to the zero-egress sign-only surface + a copy-pasteable usage guide (provision→deploy→client loop) + verified API/CLI/env tables. Documentation only — `1.1.0a16-readme-current.md`
- **v1.1.0a17** — docs/ tree reconciled: the docs the README links to were also stale (~2,600 lines across architecture/decisions/threat-model/dependencies/implementation-plan still described Vault custody + the `sign-and-send`/broadcast model as current). Rewrite-to-current (architecture.md heavy; superseded markers on decisions D4/D6/D10; threat-model A5–A8 retired-Vault banner; dependencies RPC→caller-prereqs; impl-plan zero-egress track). History preserved per Core #18; claims grepped against `src/`. Documentation only — `1.1.0a17-docs-tree-reconciliation.md`
- **v1.1.0a18** — external-review doc fixes: an independent read-only review against `src/` caught ~25 doc↔code mismatches a16/a17 missed — `DATABASE_URL` env var, the `{tx_id,hash,signed_raw_tx,nonce}` sign response, master-key **per-operation** load (not "once at startup"), the FastAPI `{detail:{error,message}}` envelope, residual `sign-and-send`/`sign_and_send.py`/Vault-AppRole spots in CLAUDE.md + decisions + threat-model, and stale incident-time runbooks (banners added). Documented the replacement-lifecycle limitation + flagged 3 code gaps (replacement, litestream egress, tenacity). All re-verified against code. Documentation only — `1.1.0a18-review-fixes.md`
- **v1.1.0a19** — replacement lifecycle fix (code; closes a18-flagged gap #1): `sign-replacement` now keeps the tx `submitted` (deleted the terminal `update_status(tx_id,"replaced")`), so the client reports the replacement's receipt via the normal `/receipt` (accepts any recorded hash) — nonce confirms, no `nonce-sync` — and the ≤5-retry cap is reachable. `replaced` is now vestigial. Sonnet-implemented in an isolated worktree + Reviewer binding pass; +2 tests, 476/1 green — `1.1.0a19-replacement-lifecycle-fix.md`
- **v1.1.0a20** — litestream egress lockdown (config; closes a18-flagged gap #2): pinned the `litestream` sidecar `network_mode: none` in `docker-compose.yml` — it replicates over the shared `fwd-state`→`backup` volumes and needs no network, so the whole stack is egress-free (previously it silently joined Compose's default egress-capable bridge). `docker compose config` validates — `1.1.0a20-litestream-egress-lockdown.md`
- **v1.1.0a21** — tenacity removal (deps; closes a18-flagged gap #3, last of the three): dropped the dead `tenacity` dependency (zero use since the a9 RPC excision; not a transitive dep) from `pyproject.toml` + `poetry.lock` via a minimal `poetry lock --no-update` (only `tenacity` removed, 75→74 packages, no other version churn). Suite 476/1 green. All three review-surfaced code gaps now closed — `1.1.0a21-tenacity-removal.md`
- **v1.1.0a22** — Core #18 amended + CLAUDE.md sanitized (constitutional; D21): the current-state docs now describe the present only — no "was X / retired / superseded / fixed in vX" archaeology, no version arrow-chains; history lives only in the quarantined record (this dir + `decisions.md`). Collapsed the §Scope a8→a21 arrow-chain; stripped Vault/version asides from the invariants + IS-NOT + carryover; deleted the roadmap rollups. (Content docs = a23, dead code/config = a24.) — `1.1.0a22-current-state-docs-amendment.md`
- **v1.1.0a23** — content docs sanitized to present-tense (per a22/D21): README + architecture.md + threat-model.md + dependencies.md stripped of all Vault/Scaleway/sign-and-send/version archaeology. architecture.md fully rewritten (Sonnet in worktree + Reviewer binding-pass); threat-model dropped the retired-Vault attacks A5–A8 + tiers 6–7 and renumbered; dependencies dropped the struck rows + the Phase-1 spike section. Grep-clean (only live `vault_master_key`/`vault_unavailable` symbols remain). Documentation only — `1.1.0a23-content-docs-sanitized.md`
- **v1.1.0a24** — dead code/config removed: deleted the retired Vault-snapshot sidecar (`config/vault-snapshot/` + `scripts/vault-snapshot*.sh`) + 5 stale Vault/sign-and-send-era runbooks; dropped the vestigial `replaced` tx status from `_VALID_STATUSES`; cleaned orphaned `.pyc`. ruff/mypy/476-1 suite green, compose valid. Flagged: the remaining code-internal Vault naming (symbol renames + the `vault_master_key` column migration + the `vault_unavailable` error-code contract + code docstrings) is a deeper refactor, operator-gated — `1.1.0a24-dead-code-config-removed.md`
- **v1.1.0a25** — fixed dangling refs to the a24-deleted runbooks: `architecture.md` ("canonical in `runbooks/restore.md`") + 4 cross-refs in `v1.0.0a1-sealed-master-verification.md` to `sign-and-send-verification.md`/`phase-7-verification.md`. Current docs + kept runbooks are link-clean. The code-internal Vault-naming refactor (a26) stays flagged — `1.1.0a25-fix-deleted-runbook-refs.md`
- **v1.1.0a26** — QA-driven sanitization reconciliation: closed an independent QA's list against a22–a25 — quarantined the two stale Vault/`sign-and-send` runbooks into this dir (no more `docs/runbooks/`), made §Scope versionless (kills the recurring stale-version drift), tightened audit-coverage overclaims to "every signing request/decision/admin-mutation/reported-outcome", re-added the load-bearing `eth-abi` + `cryptography` deps a23 over-pruned, fixed the architecture status-enum + domain layer-boundary, and swept residual version/Vault archaeology out of `.env.example` + `config/abis/README.md` + `docker-compose.yml`. Code: dropped vestigial `dropped`/`sign-and-send` enum values. 476/1 green, compose valid. Code-internal Vault/`sign-and-send` symbol naming stays the deferred operator-gated refactor — `1.1.0a26-qa-reconciliation.md`
- **v1.1.0a27** — QA reconciliation stragglers: the two findings a26's grep pattern didn't land — scoped the `architecture.md` "Every failure writes an audit row" overclaim to the sign path (pre-handler/health/read rejects write no row) and added the omitted `types-pyyaml` dev-tooling row to `dependencies.md`. Doc-only — `1.1.0a27-qa-reconciliation-stragglers.md`
- **v1.1.0a28** — security fix (custody/Core #12): stopped the Dockerfile baking secrets — `COPY config/` → `COPY config/abis/` (a whole-project review caught it; verification also found 8 `config/policy.yaml.bak-*` files committed to the public repo in the a24 commit + baking into CI images). Verified via real build that the image now ships only `abis/`, zero `master.key`/`policy.yaml`/`.bak`. Added `.dockerignore`/`.gitignore` guards; `git rm --cached` the backups. No master rotation needed (never pushed; master lives on-host by design; no creds in the backups). — `1.1.0a28-image-secret-bake-fix.md`
- **v1.1.0a29** — policy fail-closed hardening (the a28-deferred review findings; D22): (1) `ContractRule.chains` REQUIRED — step 2 denies when `request.chain` isn't bound (a contract address isn't chain-unique); (2) `MethodRule.allow_unconstrained_args` — step 4b denies methods with non-scalar array/tuple args unless opted in; (3) step 9 + loader check 4b require every EVM-allowlisted wallet to have a `policy.wallets` binding; (4) idempotency replay 409s on a same-key/different-body reuse instead of returning the wrong cached tx. 484/1 green, ruff+mypy clean. Breaking for the live `policy.yaml` — operator must add `chains`/`allow_unconstrained_args`/wallet bindings before redeploy (D22 follow-ups). — `1.1.0a29-policy-fail-closed-hardening.md`
- **v1.1.0a30** — `clifwd policy validate [--policy] [--schema-only]`: runs the daemon's startup checks (`load_policy` + `check_consistency`) without starting the daemon, so an operator can validate a `policy.yaml` edit BEFORE `docker compose up -d` recreates the container (exit 0 valid / 2 invalid). New `cli/policy.py` + `app/policy_check.py` (cli->app boundary, mirrors `wallet_import`'s re-export). Reuses the existing loader — no new validation logic. First provider-distribution Phase-0 ship; `policy init` generator is next. 488/1 green, ruff+mypy clean; live-validated against the production policy (9 callers/6 perms, VALID). — `1.1.0a30-policy-validate-cli.md`
- **v1.1.0a31** — `clifwd policy init --networks <list> [--capabilities claim,fsp] [--recipient]`: generates a correct a29-schema `policy.yaml` (required `chains`, `allow_unconstrained_args`, recipient pin, `fsp_self_submit` carve-out, `wallet_constraints`) so providers don't hand-author the error-prone schema. Method sigs DERIVED from the shipped ABIs (can't drift). New `app/policy_init.py` + `config/networks.yaml` (generator-only DATA; daemon stays chain-agnostic; explicit `COPY config/networks.yaml`, not broad). Generated output round-trips through `load_policy` + `check_consistency` → 0 errors. 494/1 green, ruff+mypy clean. — `1.1.0a31-policy-init-generator.md`
- **v1.1.0a32** — provider-distribution surface (docs + scaffolding, no daemon change): rewrote `docs/one-command-install.md` to the approved design (sealed-master not Vault, single-host positioning, fwd-core + `--with-clif`, build-from-source, custody gate, phased onboarding); added the `--with-clif` overlay `docker-compose.clif.yml` + host `install/fwd`+`install/clifwd` wrappers; genericized AP→operator framing across README/architecture/dependencies/threat-model + example-policy recipient placeholder (honest-history docs untouched). Merged compose validates (fwd+litestream+clif-auto+clif-fsp-auto). `install.sh` + hosting is Phase 1. — `1.1.0a32-provider-distribution-surface.md`
- **v1.1.0a33** — `install/install.sh`: the k3s-style `curl|sh` bootstrap (build-from-source, single-host). Preflight → clone pinned source (`FWD_REF`/optional `FWD_SHA`) → build → generate admin key + inert default-deny `version:1` policy + sealed master (locally, never transmitted) → install host `fwd`/`clifwd` wrappers → `up` inert + health → custody-gate next-steps. Flags `--with-clif`/`--no-start`/`--no-build`/`--production`/`--dir`/`--ref`; idempotent. Verified via a `--no-start` staged run (build + 0600/32B master + inert policy schema-VALID + wrappers); live `fwd` untouched. Hosting + fetched-manifest checksums + clif-public are the remaining Phase-1 sub-steps. — `1.1.0a33-install-sh-bootstrap.md`
- **v1.1.0a34** — configurable container name `${FWD_CONTAINER:-fwd}` in `docker-compose.yml` + `install/clifwd` + `install/install.sh` (default unchanged → production `fwd`/`fwd-litestream` unaffected). Enables an isolated second instance (own name + `COMPOSE_PROJECT_NAME`-scoped volumes/network) so install.sh's real `up`+health path can be verified locally without colliding with a running production `fwd`. — `1.1.0a34-configurable-container-name.md`
- **v1.1.0a35** — ran install.sh's real `up`+health locally (isolated `fwd-localtest`/`fwdlocaltest` alongside untouched production) → healthy, default-deny (401), then full custody-init on it. The run caught + fixed two defects staged testing couldn't: (1) `.env`/config must live in the compose dir `$SRC` (was `$FWD_DIR` → `env file not found`); (2) the custody-gate ORDER — `fwd restart` to LOAD the policy must precede `wallets/callers create` (admin validates `policy_path` against the loaded policy; was failing `400 unknown_policy_path`). Fixed install.sh banner + `one-command-install.md`. Production untouched (180 rows). — `1.1.0a35-install-local-verified-fixes.md`
- **v1.1.0a36** — ran install.sh `--with-clif` locally (isolated `fwd-cliftest`/`fwdcliftest`, local clif source) → built BOTH images (`fwd:cliftest`+`clif:dev`), fwd healthy, and **clif→fwd reachability proven** (one-shot `clif health` → `http://fwd:8080/healthz` 200 through the dual-network wiring). Fix it required: seed a placeholder clif `.env` from `.env.example` after the clone (compose validates env_file for the stopped clif daemons too → `up` was aborting `env file not found`). Production untouched. — `1.1.0a36-with-clif-local-verified.md`
- **v1.1.0a37** — documentation professionalization (multi-agent cleanup workflow + Reviewer binding-pass): brought all current-state docs current/consistent/professional and fixed the public-repo drift — `install.sh` default `FWD_REPO` → the public `github.com/africanproofs/fwd`, Core #12 wording → github canonical-public, and README/architecture/dependencies/one-command-install reconciled to the build-from-source model (GitLab registry is optional/internal, not the install path). All 19 invariants + the "What FWD IS NOT" list + workflow/commit doctrine preserved; no append-only file touched; no AI attribution. 494/1 green, ruff+mypy clean. clif got the parallel pass (v0.5.8). — `1.1.0a37-docs-professionalize.md`

## Quarantined operator-procedure snapshots

Point-in-time operator procedures that no longer match the current code, kept here as history (not current-state runbooks — there is no `docs/runbooks/` directory):

- `ci-integration.md` — the earlier CI integration procedure. The current CI is `.gitlab-ci.yml` (it runs `poetry run pytest tests/integration/`).
- `v1.0.0a1-sealed-master-verification.md` — the one-time sealed-master custody verification at v1.0.0a1; written against the then-host-port deployment + `/v1/sign-and-send`, neither of which exists now.
