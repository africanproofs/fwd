# v1.0.0a1 — Vault retired; sealed local master (Phase 8a)

**Shipped**: 2026-05-17
**Author**: Opus 4.7 Reviewer prescribed the canonical prompt; **Sonnet 4.6 implementer subagent** built code + tests; Reviewer did the binding pass + in-ship doctrine. Operator-gated the prompt + the Vault decision.
**Predecessor**: v0.5.6 (`1421f96`, local-only, push blocked).

## Decision

After the operator's asset-class reframe (~1000 FLR ≈ tens of USD per key; Flare automation wallets — FTSO-claim/registration/gas, never treasury; host **never publicly exposed**) and two independent audits ("process has become the product"; Vault's one differentiator — an independent decrypt-audit-trail — worthless here and never enabled per v0.5.5 OE-2), the operator ruled: **replace HashiCorp Vault with a sealed local master.** Phase 8 unfreezes doctrine for in-ship reconciliation.

## What shipped

`SealedMaster` (`src/fwd/infra/sealed_master.py`): AES-256-GCM (`cryptography` AESGCM, mirrors the retired Vault `aes256-gcm96`), 32-byte master from a mode-0600 host file owned by the `fwd` user, `seal:v1:<b64(nonce‖ct)>` ciphertext. Same async surface as the retired `VaultClient` — the `Signer` seam, policy, intent, audit, nonce, API are **untouched**. `clifwd master generate`. `cryptography ^44.0` promoted to an explicit pinned dep (custody primitive — it was not even transitively present; the Phase-0 assumption it was at 2.8 was system-python, not the venv). Regenerate-not-migrate (no production wallets exist; testnet wallets disposable).

**Deleted** (net-negative surface): `vault_client.py`, `scripts/vault-init.sh`, `scripts/ci_vault_init.py`, `config/vault/`, the `vault` + `vault-snapshot` compose services + `fwd-internal` net + vault volumes, the CI vault stage, `test_vault_client.py`.

## Workflow + the Reviewer correction

Reviewer prescribed `~/.claude/plans/fwd-canonical-prompt-v1.0.0a1.md` (HAND-OFF fenced) + wrapper; operator gated; Sonnet implemented cold; Reviewer did the binding file-by-file pass and independently re-ran every gate (never trusting the self-report — Core #14, doubly so on the custody path). **One Reviewer-owned correction (v0.4.0a5 precedent, documented):** `src/fwd/api/health.py` was entirely Vault-coupled (`/healthz` `vault` field probed `http://vault:8200/v1/sys/health`) — a gap in the Reviewer's canonical prompt (it scoped docker-compose but not the app healthcheck), **not** a Sonnet failure. Fixed in place: `vault` field → `master`, a pure stat-only sealed-master readiness probe; `test_health_endpoint.py` updated; `clifwd health` is shape-agnostic (no change). Sonnet's three surprises were all legitimate (the `cryptography`-not-transitive finding was *better* than the Phase-0 assumption; the test approach was spec-permitted; the version-consistency note was correct — the prompt's warning was imprecise). Verdict **ACCEPT**, no path-b.

## Gates (independently re-run by the Reviewer)

426 passed, **0 skipped** (was 415 + 4 Vault-skip; the 4 ex-skipped integration tests now run unconditionally — no external dep). ruff / format / mypy --strict (52 files) / layer / version-consistency green; `docker compose config` OK; zero functional `vault` references (only intentional historical-naming docstrings). Every touched doc byte-verified NUL=0 / `file`→text on the committed blob (the v0.5.5 lesson).

## Doctrine reconciled IN-SHIP (freeze permits Phase-8 in-ship)

Security-truth-critical (now-false claims — Core #18 mandatory): CLAUDE.md § Identity + Core invariants **#1** (Vault Transit/`exportable=false`/`transit/decrypt` → AESGCM 0600-file sealed master + honest blast radius), **#8** (Vault Shamir unseal → no unseal ceremony), **#17** (Vault wipe-and-redo → custody proportionate to the asset class; the operator's ruling, the audits' basis); `decisions.md` D1 (superseding decision; the Vault v0.1.2–v0.5.6 body kept as honest history per the v0.4.3 precedent); `architecture.md` (trust-boundary row, § Signing-flow intro, § Vault configuration → § Master key); `threat-model.md` (asset table, A3); `dependencies.md` (vault/vault-snapshot rows removed, cryptography reality).

**Mechanical-mirror tail — explicit in-Phase-8 deferral (Core #18 named deferral, NOT silent drift):** residual "Vault decrypt"/"transit/*"/"fwd-master" wording in architecture.md signing-flow numbered steps, the `wallets` schema comment, the wallet-provisioning step text, § Auth lifecycle (D10 Vault token), threat-model A4/A5 deep prose, and the component-topology ASCII. These restate the same substantive (now-correct) security model in stale mechanism words; they carry zero false *security* claim. Closed with or before Phase 8b — they do **not** ship as silent drift.

## Next

Live Coston2 sealed-master sign gate (Core #14, operator-run, mirrors v0.3.1) — a real tx mined with `from` recovering to the wallet, proving the sealed-master custody path end-to-end; recorded as the verification-met addendum, does not block this code commit. Then **Phase 8b**: the actual `ftso-fee-claimer` cutover and the first deleted `.env PRIVATE_KEY=` line (v1.0.0; lifts the freeze).

## Commit

Single terse conventional-commit line, NO trailer, operator sole author (root constitution NON-NEGOTIABLE). Committed local-only; push remains blocked by the GitLab account block; the a3→v1.0.0a1 stack is locally unpushed, `origin/main` at `f1ee3f1`.

## Verification-met addendum (recorded at v1.0.0a3, 2026-05-18)

**v1.0.0a1 Core invariant #14 gate MET — live Coston2, 2026-05-18.** The stack was recreated from the v1.0.0a1 tree (`docker compose ps` = `fwd` + `fwd-litestream` only — **no `vault`, no `vault-snapshot`, no `fwd-internal` net**). `/healthz` → `{"master":"ok","rpc":...,"fwd":"ok"}` (the field is `master`, not the retired `vault` — the v1.0.0a1 Reviewer-owned health.py rewrite is live). Wallet `sealed-verify` = `0xf49C60A324553956D197C4C5Bc15B57b0Adb4231`, privkey stored as `seal:v1:…` (88-char b64 of 12-byte nonce ‖ ct ‖ GCM tag — no `vault:`, no plaintext), `wallets.vault_master_key` column = `local:v1`. A real Coston2 tx via `POST /v1/sign-and-send` (caller-token-gated, default-deny policy enforced) — `tx_id` `019e39ba-2aed-7c43-a655-181dbff3a7f4`, hash `0xc222bf62d30017daf67f42d472677ee0ffe6afb35592c0895a4c1d957b5ac5e6` — mined at **block 30662555**, `status=0x1`, `type=0x2` (EIP-1559), `gasUsed=21560`. **On-chain `from` (secp256k1-recovered by Coston2) = `0xf49c60a324553956d197c4c5bc15b57b0adb4231` = `$ADDR` (case-insensitive match).** That recovery is impossible unless `SealedMaster.decrypt()` AES-256-GCM-decrypted the exact privkey from the wallet's `seal:v1:` ciphertext under the mode-0600 host-file master and `eth-account` signed with the recovered key — **the sealed-master custody chain is proven end-to-end with zero Vault anywhere in the path.** `docker exec fwd clifwd audit verify` → **chain intact: 37 rows, exit 0** (the D16 walker over the Vault→sealed-master swap + this drill's policy-load / wallet-create / caller-create / the denied step-3 row / the approved sign-and-send row). All pass/fail rows (0, 0.5, 1, 5, 6, 7) met.

**Drill value (Core invariant #14 earned its keep again — cf. v0.4.5, v0.5.2, v0.5.3):** the live run surfaced a **runbook-shape defect** (not a code defect): the v1.0.0a2 runbook's premise that an empty-`data` value send exercises the custody path is wrong — Core invariant #3 ("sign intent, never opaque bytes") correctly **denies** undecodeable calldata at policy step 3 (`policy_denied step=3: calldata too short`). There is no policy-passable bare value transfer through `/v1/sign-and-send` by design. The runbook (Step 2 policy, Step 2 note, Step 5 curl, pass/fail row 5: `201`→`200`) was corrected in place at v1.0.0a3 to the proven `phase-7-verification.md` shape — a self-addressed ERC20 `transfer(address,uint256)` with `amount=0` (the EOA-target premise is the same operator-gated one phase-7 used: the gate verifies fwd's decode→policy→sealed-master-decrypt→sign→broadcast→custody path, not token semantics). The custody property under test is unchanged. v0.5.1 precedent: drill surfaces doc/substrate drift → fix folded into the evidence ship.

**Phase 8a verification complete.** **Phase 8b** (`ftso-fee-claimer` cutover; first `.env PRIVATE_KEY=` deletion; v1.0.0 — lifts the doctrine-ship freeze) is **unblocked** (still gated by Core invariant #15 operator approval). Recorded local-only; push remains blocked by the GitLab account block. The `sealed-verify` wallet + `sealed-verify-caller` + the minimal `config/policy.yaml` (gitignored, Core #12) are the drill's live state; the operator's pre-v1.0.0a1 phase-7 policy is preserved at `config/policy.yaml.pre-v1.0.0a1.bak`.
