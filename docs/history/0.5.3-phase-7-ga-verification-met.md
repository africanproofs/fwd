# v0.5.3 — Phase 7 GA verification gate MET live on Coston2 (Reviewer-only addendum)

**Shipped**: 2026-05-17
**Author**: Opus 4.7 Reviewer. No Sonnet; no code/test change. Records
the on-chain + audit evidence that the Phase 7 GA gate
(`docs/runbooks/phase-7-verification.md`) passed live, exactly as
v0.4.1 recorded the Phase 5 proof and v0.4.6 the Phase 6 proof.
**Predecessor**: v0.5.2 (`92786ce` — drill-surfaced D16/Core-#5
deny-audit fix; committed local-only, push blocked by the GitLab
account block).

## Outcome

The operator-gated **literal full 10-vector D14 deny matrix** + the
approved/mined, idempotency-replay, admin-audit, and chain-verify
steps are **all proven live on Coston2**. Phase 7 GA is **MET**. The
drill performed exactly its Core-invariant-#14 function: across
v0.5.1/v0.5.2 it surfaced eight substrate drifts and one real
D16/Core-#5 defect (denied/error audit rows rolled back) — all fixed
and re-verified live before this gate was declared met.

## Evidence (Coston2; gate policy = `config/policy.yaml`, gitignored)

Gate wallets (public — Vault-custodied): `phase7-gate`
`0xa1c51C9E76B0b10195EA81c0c940445Da55bb5a7`; `phase7-wr-wallet`
`0xa2b394978DfAE05f8b23d50C260B3dcd8A5d2b34`; `phase7-offlist`
`0x065821A3db5f718f62F4dC2b900B03ee3646a530`.

| Vector | D14 step | Live result |
|---|---|---|
| V1 | 1 caller→binding | orphan-caller policy → `SystemExit(1)`; forensic `policy-load error` row (seq 8) survived the exit; chain intact after restore |
| V2 | 2 contract not permitted | HTTP 403 `step=2`; denied row persists |
| V3 | 3 decode fail | HTTP 403 `step=3` (truncated calldata); denied row persists |
| V3b | 3 unknown selector | HTTP 403 `step=3` (garbage); denied row persists |
| V4 | 4 method not in policy | HTTP 403 `step=4` (`approve`); denied row persists |
| V5 | 5 value > max | HTTP 403 `step=5`; denied row persists |
| V6 | 6 arg predicate | HTTP 403 `step=6` (wrong recipient); denied row persists |
| V7 | 7 wallet not allowlisted | HTTP 403 `step=7`; denied row persists |
| V8 | 8 caller rate | same-hour 2nd call → HTTP 403 `step=8` (v0.5.1) |
| V9 | 9 wallet rate | call#1 ALLOWED → mined `status=0x1`, `from`=`0xa2b3…2b34`, block **30623108**; call#2 → HTTP 403 `step=9: wallet constraint exceeded`; both audit rows persist (seq 24 approved / 25 denied) |
| V10 | 10 Allow | approved → mined `status=0x1`, `from`=`0xa1c5…b5a7`, block **30596205**; nonce advanced |

For V2–V7 the gate-wallet on-chain nonce stayed `0x0` — **no denied
request broadcast**.

**Custody recovery (Core invariant #14, the strong proof):** two
distinct Vault-custodied wallets each had their on-chain `from`
recovered by Coston2's secp256k1 signature recovery and matched the
address fwd custodies — V10 (`phase7-gate`, block 30596205) and V9#1
(`phase7-wr-wallet`, block 30623108). The full custody chain
(Vault-encrypt → in-process decrypt+sign+zeroize → broadcast → on-chain
recover) holds end-to-end.

**Idempotency (D14):** replaying the V10 request with the same
`Idempotency-Key` returned the SAME `tx_id`
(`019e30a4-07b8-7aa9-b05b-56df5863ed4a`), produced **no second
broadcast** (pending nonce unchanged), and wrote a
`sign-and-send-duplicate` row with `outcome.original_tx_id` +
`original_audit_seq=13`, `decision_reason=idempotency_replay`.

**Admin-action authorship (D16, a7):** 3 `wallet-create` + 3
`caller-create` rows, `caller=NULL`, `outcome` = `address`/
`api_key_prefix` only; a full audit dump scanned against the THREE
real generated api_keys + `fwd_live_`/`$argon2`/`api_key_hash`/
`privkey`/`vault:v1:` → **all absent**. a7 admin policy_path
validation: a bad `policy_path` → HTTP 400 `unknown_policy_path` with
**no audit row** (rejected before the use case).

**Hash chain (D16):** `docker exec fwd clifwd audit verify` →
**"chain intact: 25 rows", exit 0** — the canonical D16 walker command
(restored by the v0.5.1 shim), over policy-load + every denied +
approved + duplicate + admin row, including the v0.5.2-fixed denied
rows woven in.

## Drill value (Core invariant #14)

The live gate surfaced what unit tests could not:

- v0.5.1 — eight doctrine↔substrate drifts (stale pre-Phase-7 image;
  Dockerfile missing `COPY config/`; compose missing
  `FWD_POLICY_PATH`/policy mount; stale `poetry.lock`; no `sqlite3`;
  prior active callers; `clifwd` not on PATH; EOA token) — all fixed.
- v0.5.2 — a real D16/Core-#5 defect: denied/error sign-and-send
  audit rows were rolled back when the exception propagated through
  `session_scope` (same defect class as the v0.5.0a6 `main.py`
  SystemExit bug, recurring for `sign-and-send`; unit-invisible
  because the a6 suite mocked the session) — fixed
  (`AuditRepo.commit()` + commit-then-raise) and re-verified live
  (seq 15→23, seven `denied` rows now persist).

Without the live drill, the deny-audit defect would have shipped to
the v1.0.0 production migration. This is the third time the
operator-driven gate caught a unit-invisible defect (v0.4.5
concurrency; v0.5.1 substrate; v0.5.2 deny-audit).

## Closes

- Audit deferrals **F2.1 + F8.2** (the Phase 7 synthetic-attack
  default-deny + `clifwd audit verify` gate) — closed by this live
  pass.
- **Phase 7 GA is MET.** The policy engine, ABI intent decoder,
  hash-chained audit log, default-deny path, idempotency, and
  admin-audit authorship are all verified against live Coston2 + the
  real Vault custody path.
- **Phase 8 (v1.0.0 — `ftso-fee-claimer` production migration) is
  unblocked.** Note Core invariant #17: the production cut requires
  the wipe-and-redo of the dev Vault (the dev unseal-shortcut file is
  forfeit) before any production privkey is custodied.

## Commit

Single terse conventional-commit line, NO co-author trailer, operator
sole author (root constitution NON-NEGOTIABLE rules). Committed
local-only; push remains blocked by the GitLab account block on
`@makhosonke`; the a3→v0.5.3 stack is locally unpushed, `origin/main`
at `f1ee3f1`.
