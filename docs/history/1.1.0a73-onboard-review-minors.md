# v1.1.0a73 — onboard review MINORs applied (cutover authorization hedge + gas-wallet rationale)

**Type:** presentation-only `install/onboard` follow-up to a72's adversarial review. No custody / signing / nonce / policy / clif-env / daemon / money-path behaviour change.
**Date:** 2026-06-04.

## Why

The a72 binding-pass review (SAFE) flagged two optional MINOR polish items; the operator authorized applying them in full.

## What changed (`install/onboard` only — 2 `printf` additions + a suffix)

- **MINOR-1 — `--import-existing` cutover hedge.** The cutover branch asserted the imported keys are already live on-chain and printed no authorization steps. Added two lines so a user importing a not-yet-authorized key is pointed at the greenfield steps: `ClaimSetupManager.setClaimExecutors · setAllowedClaimRecipients · register the FSP signer as your voter address.` Prints unconditionally in the import-existing path.
- **MINOR-2 — gas-wallet rationale.** The greenfield `fund gas wallet: …` lines regained the dropped rationale via a `— pays tx gas` suffix (no extra format specifier; the two `%s` still bind the wallet name + address).

## Validation

`sh -n` + `dash -n` pass. `git diff` is two added `printf` lines plus a suffix on two existing `printf` format strings — every changed line is an output string; no control flow, variable, command, wallet/policy/nonce/token/clif-env/daemon change. Format-specifier counts unchanged (no `printf` arg mismatch). Every safety gate remains unconditional (a72 trace unaffected). The added cutover lines print in both compact and guided modes.

## Files

`install/onboard`, version files.
