# v1.1.0a5 — Bounded post-verification reconciliation: FSP doctrine ⇄ byte-verified code

**Date:** 2026-05-19
**Reviewer:** Opus 4.7
**Implementer:** — (doctrine + config only; the Reviewer authors the
constitution directly per § Development workflow — no Sonnet hand-off, no
`src/` production code change; one stale test-fixture assertion updated, see
below)
**Authorization:** operator, explicit, 2026-05-19 ("lets go with your
recommendation"), Reviewer-prescribed with the real drill facts in hand
(the P3=NO branch of the forward plan's step-4 decision-point — not
pre-decided).

## What shipped

The first **bounded post-verification reconciliation** ship of the D17
ship-type (D17 itself was genesed at v1.1.0a3; v1.1.0a3/a4 were
constitutional-amendment ships). It reconciles the FSP doctrine surface to
the v1.1.0a1/a2 byte-verified FSP code on the strength of the v1.1.0a4 live
Coston2 drill.

- **CLAUDE.md Core #3** — reframed from "every signing request must decode
  to a known ABI shape" to: fwd signs ABI-decoded transactions **OR** EIP-191
  personal-signed structured protocol messages whose 32-byte body fwd
  reconstructs from policy-checked typed fields; callers never supply a raw
  digest; safety = byte-verified preimage + EIP-191 domain prefix +
  address-level key-domain segmentation.
- **CLAUDE.md:49 ("No raw-digest signing")** — clarifying note: the route
  set is now `/v1/sign-and-send` + `/v1/sign-fsp-message`; the latter is
  EIP-191 `personal_sign` over a fwd-**reconstructed** messageHash (no caller
  digest), so "arbitrary `eth_sign`-style raw-digest endpoints will never
  exist" stays **literally true**. The stale "mounts only
  `/v1/sign-and-send`" wording (v1.1.0a2 drift) is reconciled here.
- **CLAUDE.md Core #17** — appended the FSP signing-policy hot-key honesty
  sentence: proportionality holds, but recovery is **not "free"** (depends on
  verified on-chain signing-policy re-registration; the drill established
  P3=NO), and compromise can produce valid FSP signatures until detected and
  rotated + re-registered. The existing v1.0.0 DR-mechanic flag is untouched
  (Core #18 — no rewriting an unverified mechanic).
- **CLAUDE.md § Scope** — current line advanced a4 → a5 (single line; no
  per-version narrative paragraph — D17 constraint iii).
- **docs/decisions.md** — **D19** (the GATE-1 outcome + the narrowed
  reconciliation decision; the honest F2/P3=NO deferral; the bounded
  surface).
- **docs/architecture.md** — `/v1/sign-fsp-message` API-surface row.
- **docs/history/README.md** — one index line; this deep file.
- **pyproject.toml** + **src/fwd/version.py** — `1.1.0a5`.

Bundled (deployed-but-untracked config the v1.1.0a2 code already loads at
runtime — itself minor drift, closed here): `config/abis/flare_systems_manager.json`
+ `config/abis/registry.yaml`. Plus `docs/reviews/v1.1.0a4-clif-fsp-live-drill-adjudication.md`
— the GATE-1 evidence and the D17(ii) inline-annotation citation source.

**Honest scope deviation (surfaced, not hidden — Core #18).** Bundling the
runtime-loaded FSM config required updating one stale test-fixture assertion,
`tests/unit/test_abi_registry.py::test_load_real_abis_succeeds_with_correct_names`
(the ABI-name-set pin), to include `flare_systems_manager`. That assertion
should have moved when the FSM ABI was added as the live-drill prerequisite;
it did not, so bundling the config here surfaces and closes it. This is the
*only* code-tree touch — no `src/` production code is modified. The
"doctrine + config only / zero code change" framing is corrected accordingly
throughout this ship (§ Scope line, D19) rather than left silently false.

Every doctrine edit is annotated inline "code in v1.1.0a1/a2 … makes this
true" (D17 constraint ii); no § Scope narrative paragraph (iii); ONE
coherent ship (iv); only invariant text the byte-verified code makes true,
nothing GATE-pending (i).

## The drill it reconciles (independently Reviewer-adjudicated)

Full record: `docs/reviews/v1.1.0a4-clif-fsp-live-drill-adjudication.md`.
clif self-reports were **not** the verdict — the Reviewer recomputed
preimages from the Step-0 spec, re-ran EIP-191 recovery, re-decoded calldata
from the Coston2 chain, and decoded reverts (Core #14, the
spawned-implementer control).

- **Test A — fee-claimer regression: PASS.** No regression from the FSP
  additions (new ABI, FSP policy blocks, migration 0006, address-level
  segmentation).
- **GATE-1 F1 — CLEARED.** fwd's UPTIME messageHash byte-equals an
  independent from-scratch Step-0 recompute **and** the frozen Step-0 KAT
  `0xb7e97e6b…f67d`; `fakeVoteHash` byte-equals `0x290decd9…e563`.
- **B1 — clif↔fwd Leg-1 PASS.** EIP-191 recovery == the segmented
  `clif-fsp-signing-policy` address; clif keyless.
- **B2(i) — clif↔fwd Leg-2 PASS to signed-broadcast.** fwd
  authenticated + policy-passed + ABI-decoded + segmented-signed + broadcast
  `signUptimeVote`; chain rejected solely on the unfunded sender (named
  confound, not a defect); Core #19 forensic row recorded.
- **GATE-1 named residual — substantially closed.** The live
  `FlareSystemsManager` (the web3-v4 consumer itself) recovers fwd's
  eth-account EIP-191 signature; a zero-s contrast proves the registry check
  is reached only via successful recovery. Real-binary diff remains
  belt-and-suspenders, no longer load-bearing.

## Honestly NOT shipped here

- **GATE-1 F2 — on-chain FSP-protocol acceptance.** Drill established
  **P3=NO** (`signUptimeVote` reverts `'invalid signing policy address'`
  after recovering fwd's signer — AP is not a registered Coston2
  signing-policy voter). Doctrine does **not** assert on-chain acceptance;
  it is explicitly marked deferred. An on-chain-identity matter, not a
  clif/fwd defect. A follow-up bounded reconciliation lands the F2 clause
  when AP registers an FSP signing-policy entity (no re-authorization
  needed — same D17 ship-type).
- **Core #7 Fast-Updates-VRF carve-out.** Deliberately out of scope —
  parked 10x; the signing-tool path is keyless-via-fwd and *strengthens*
  #7. This is the "narrowing" the P3=NO branch prescribes.
- **clif `.env PRIVATE_KEY=` deletion / production cutover.** clif's
  decoupled operator-gated milestone (Core #15) — unaffected.

## Verification

Doctrine + config + one test-fixture assertion — no `src/` production code
change; the v1.1.0a4 live drill **is** the real-infrastructure verification
(Core #14). Full unit suite **458 passed / 1 skipped** (identical to the
v1.1.0a4 baseline — the updated `test_abi_registry` pin was already a passing
test, now asserting the correct set); `test_version_consistency` green (both
version files bumped together — Core #13); `ruff` / `mypy` clean on the
touched test. The FSM ABI/registry now in-repo match what the running
v1.1.0a4/a5 daemon already loads.
