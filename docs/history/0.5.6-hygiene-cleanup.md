# v0.5.6 — Bounded hygiene cleanup (Reviewer-only; the last doctrine ship before Phase 8)

**Shipped**: 2026-05-17
**Author**: Opus 4.7 Reviewer. No Sonnet; no new code paths; no new doctrine. Defect removal only.
**Predecessor**: v0.5.5 (`904030f`, local-only, push blocked).

## Why

The **second independent audit** (`docs/reviews/v0.5.5-second-independent-audit{,-report}.md` — decision-forcing; it audited v0.5.5 *and the Reviewer's own adjudication of the first audit*, by design, to defeat reviewer-neutralization) returned: stack mostly-right, **"process has become the product"** (converging with the first audit), 0 Critical / 3 High / 2 Medium, and all three Reviewer contests (ERC20 / `coincurve` / idempotency) independently judged **sound**. It needed to exist — it found three High defects **in the Reviewer's own v0.5.5 ship**, accepted here uncontested:

- **OE-3 — false byte-hygiene closure.** v0.5.5 claimed it closed the NUL class; it had in fact committed NUL-corrupted `docs/decisions.md`, `docs/implementation-plan.md`, and *its own history doc*. Same failure mode each time: a literal NUL written between backticks in prose *describing* NUL-escaping (the original v0.5.0a2 bug, propagated into the v0.5.4 D16 bullet and recursively into v0.5.5's own fix narrative). Byte-stripped → `U+0000` text across **all** docs; every doc is now `UTF-8 text`, NUL=0. The false closure sentence in `0.5.5-audit-reconciliation.md` was corrected **in place** (Core #18 — the ship record must not assert an untruth).
- **OE-3a** — `architecture.md` `fwd → Vault` trust-boundary still asserted "cannot extract keys", the dead pre-D1 Transit-*signing* security property. Corrected to the real envelope-decrypt blast radius (a compromised `fwd` / leaked AppRole + ciphertexts decrypts every wallet — threat-model A4/A5, Core inv #1), and tied to the OE-2 audit-device gap.
- **OE-3b** — `docs/dependencies.md` wholesale stale: vault "Custody + signing (Transit, ECDSA secp256k1)", litestream "to S3-compatible storage", phantom `hvac` row (the Vault client is hand-rolled on `httpx`), "all three" services (four), Scaleway-bucket prose. Reconciled.

## OE-2 — the Vault flip fact (verified, surfaced, NOT actioned)

The auditor's load-bearing new fact, independently verified here: **there is no `vault audit enable` anywhere** in `scripts/vault-init.sh`, `config/vault/*.hcl`, or `docker-compose.yml`. The independent decrypt-audit-trail the threat model leans on — the one custody property a Shamir-sealed local master genuinely lacks, the basis on which the Reviewer told the operator Vault was still defensible — **does not exist today**. This is the flip fact for the Vault decision and is now an explicit Phase 8 entry gate.

## Operator-authority decisions — surfaced UNCONTESTED, routed past the audited Reviewer (audit §6)

Not actioned here; the Reviewer is the audited party and does not self-rule these:
1. **Vault keep-vs-replace** + the OE-2 flip fact (keep only if durable Vault audit logging is enabled before the first production key; else replace before custody).
2. **Freeze** all non-Phase-8 doctrine/reconciliation/audit ships after this cleanup — both audits' unanimous recommendation.
3. **Local-only history** under the GitLab block (11→ commits unpushed) — explicit operator call before any production deployment.
4. **`transaction_args`** wiring as a Phase-8 task, not a fresh substrate project (forensics already covered by the audit log).

## Gates

No new code paths, no new doctrine. `pytest -q` 415 passed / 4 skipped (unchanged); ruff / format / mypy --strict / layer / version green; every doc byte-scanned NUL=0 and `file`-typed as text. version 0.5.5 → 0.5.6. Terse one-line operator-authored commit, no trailer, local-only (push still blocked).

**This is the last doctrine ship before Phase 8.** The next ship is the `ftso-fee-claimer` production cutover — the first deleted `.env PRIVATE_KEY=` line.
