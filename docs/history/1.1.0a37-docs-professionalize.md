# v1.1.0a37 — documentation professionalization + public-repo URL reconciliation

**Type:** docs ship (no daemon code change). Provider-distribution Phase 1.
**Date:** 2026-05-31.
**Authoring:** Reviewer (Opus) orchestrated a multi-agent cleanup workflow + binding-passed + applied verifier fixes; operator-gated (operator delegated decisions for this pass).

## Why

With fwd + clif now public on `github.com/africanproofs`, the current-state docs
needed a professionalization pass: bring everything current, remove stale/
contradictory wording, make it simple and logical to read, and fix the one real
drift — docs still naming `gitlab.com/proofs.africa/fwd` as the public/clone URL
when the actual public home is `github.com/africanproofs/fwd` (which the
build-from-source installer clones).

## What changed (current-state docs only — the append-only record was untouched)

- **The public-repo fix:** `install/install.sh` default `FWD_REPO` →
  `https://github.com/africanproofs/fwd.git` (CLIF_REPO was already the github
  public URL). Core invariant #12 wording in `CLAUDE.md` corrected to name the
  github repo as canonical-public (doctrine — "policy + custody, not
  concealment" + private-config-outside-repo — preserved). `README.md`,
  `docs/architecture.md`, `docs/dependencies.md`, `docs/one-command-install.md`
  reconciled to the **build-from-source** model: the operator clones the public
  github repo and `docker compose build`s locally; the GitLab container registry
  is optional/internal, not the install path.
- **Professionalization:** `README.md` (clearer intro/quickstart/provider flow),
  `CLAUDE.md` (de-duplicated prose, tightened § Scope; all 19 Core invariants +
  the "What FWD IS NOT" list + the workflow/commit doctrine preserved verbatim-
  in-meaning), `docs/architecture.md` / `docs/threat-model.md` /
  `docs/dependencies.md` (current, consistent, professional),
  `docs/one-command-install.md`, `docs/policy.example.yaml`, the `install/`
  wrappers, and `config/abis/README.md`.

## Method
A multi-agent workflow (`docs-professionalize`): parallel per-cluster cleanup
agents + an adversarial per-repo verifier (checked for accuracy drift, remaining
stale refs, forbidden append-only edits, weakened invariants, AI attribution).
Reviewer binding-passed both diffs (no forbidden/append-only files touched, no AI
attribution, all invariants intact) and applied the verifier's flagged fixes.

## Files
`CLAUDE.md`, `README.md`, `config/abis/README.md`, `docs/architecture.md`,
`docs/dependencies.md`, `docs/one-command-install.md`, `install/install.sh`,
version files.

## Verification
- `pytest` **494 passed, 1 skipped**; `ruff` + `mypy` clean; `sh -n install.sh` ok.
- Verifier verdict **pass**; Reviewer-confirmed: no append-only files touched, no
  AI attribution, all 19 Core invariants present, Core #12 = github canonical-
  public, build-from-source story coherent across README/architecture/
  dependencies/one-command-install/install.sh.
- clif got the parallel pass (its own ship, v0.5.8).
