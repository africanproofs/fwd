# v1.1.0a25 — fix dangling refs to a24-deleted runbooks

**Ship type:** doc fix (a24 deleted five runbooks; two current docs still pointed at
them). No code change.

## Fix

- `docs/architecture.md` — the backup/restore section said "canonical in
  `runbooks/restore.md`"; that file was deleted in a24. The inline summary already IS
  the procedure, so the dead pointer is removed.
- `docs/runbooks/v1.0.0a1-sealed-master-verification.md` — removed four "as proven in
  `sign-and-send-verification.md` / `phase-7-verification.md`" cross-references to
  a24-deleted files (the surrounding statements stand on their own).

## Verification

`grep` across the current-state docs + the two kept runbooks for the deleted runbook
filenames → clean. (`docs/implementation-plan.md` retains its references as honest
history — it is a quarantined history doc, not a current-state doc.)

## Still flagged (a26 — operator-gated)

The code-internal Vault naming remains: the `EnvelopeSigner` class / `self._vault`
attributes / `VaultUnavailableError` exception, the `vault_master_key` column (needs an
Alembic migration to rename), the `vault_unavailable` (503) error code (an API contract
— consumers must move in lockstep), the Vault-mentioning code docstrings/comments, and
the `ci-integration.md` runbook's stale Vault content. This is one coherent refactor
with a migration + API-contract care, deferred to its own ship.
