# v1.1.0a17 — docs/ tree reconciled to the zero-egress sealed-master reality

**Ship type:** documentation reconciliation (Core invariant #18 — doctrine must not
silently drift from code). No code, no endpoint, no behaviour change. Companion to
a16 (which fixed the public README); a17 fixes the `docs/` tree the README points at.

## Why

After a16 brought the README current, the operator flagged that the docs the README's
table links to were *also* stale. A drift audit (3 parallel read-only passes, verdicts
classified per doc) confirmed ~2,600 lines across five docs still described the
**retired Vault custody** (removed v1.0.0a1, D1) and the **`sign-and-send`/broadcast**
model (excised v1.1.0a9, D20) as if current. Operator chose **rewrite-to-current only**
(history stays in decisions.md superseded blocks + docs/history/, not duplicated).

## What changed, per doc

- **`architecture.md`** (heaviest): rewrote the one-paragraph summary, component
  topology diagram, services/networks/volumes tables, trust-boundary table, the entire
  signing-flow section (`/v1/sign-and-send`→`/v1/sign-transaction`, sealed-master
  decrypt, **no broadcast / no fee-oracle / no receipt-watcher**, client report-back +
  `/sign-replacement`), the failure-mode table, the API surface table (added
  `broadcast-result`/`receipt`/`sign-replacement`/`nonce-init`/`nonce-sync`/`nonce/holes`,
  removed/deferred-marked the unmounted ones), wallet-provisioning steps
  (`transit/encrypt`→`SealedMaster.encrypt`), the Export/DR row, idempotency
  (`sign-transaction-duplicate`), the error envelope (dropped `rpc_failed`, reframed
  `vault_unavailable`), **replaced the entire Vault-AppRole "Auth lifecycle" section
  with "There is none"**, policy-engine wiring, audit-log action enum + concurrency
  note, backup/restore (no `vault-snapshot`/Raft), and the layer-boundary + signer
  descriptions. Schema comments (`seal:v1:`, `local:v1`, status/action enums) fixed
  against code (verified: action/status literals grepped from `src/`).
- **`decisions.md`** (append-only log — history preserved): superseded markers on
  **D4** (Vault distribution), **D6** (Vault unseal), **D10** (token lifecycle) — all
  MOOT post-v1.0.0a1; fixed D3's stale "Scaleway Object Storage" clause to the v0.4.3
  local volume. D1 + D20 already correct. Historical `sign-and-send` references in
  dated decision bodies left as honest history (D20 is the rename record of truth).
- **`implementation-plan.md`**: preamble banner (read Phases 1–7 as history; Vault
  retired a1, zero-egress a9); Phase-5 receipt-watcher-retired note; version anchor
  table extended with the 9a-fsp (a1–a7) + 9b-zero-egress (a8–a17) tracks.
- **`threat-model.md`**: A3 mitigation (RPC monitoring → audit-log + zero-egress);
  a retired-Vault banner over the A5–A8 cluster (AppRole/Shamir/unseal no longer
  obtain); A9 (sealed-master at-rest, not "sealed Vault"); A10 (no `hashicorp/vault`
  image); A11 (`fwd`'s `mlockall`, not Vault's); summary table (host-root row =
  sealed-master not AppRole; struck the AppRole-leak + Shamir-share rows); attacker
  tiers 6–7 annotated retired.
- **`dependencies.md`**: RPC endpoints moved to caller prereqs (fwd has no egress);
  `httpx` reason = CLI-only; `hvac`/`tenacity` reasons corrected (`tenacity` flagged
  unused-since-a9, a removal candidate); operator prereqs (GPG/unseal/paper/IAM)
  replaced with `master.key` generation + off-host backup; honest-summary "net-new dep
  is Vault" → local `backup` volume + `master.key`; verification-spike marked Phase-1
  history. Fixed garbled "volume (v0.4.3) bucket/IAM" artifacts from a prior replace.

## Doctrine surface (reconciliation ship)

The five `docs/` files + this history file + one `docs/history/README.md` index line +
the single CLAUDE.md § Scope current-state line + the two version files. No new § Scope
paragraph. No `src/` change.

## Verification

- No NUL bytes in any reconciled doc (Core #18 byte-hygiene; checked).
- No garbled replace artifacts; no stale current-framing (`sign-and-send`,
  `transit/`, `vault-snapshot`, AppRole-as-live, Scaleway-as-live) — every residual
  `Vault` mention is explicitly retired/honest-history.
- Endpoint / CLI / status / audit-action / error-code claims grepped against `src/`
  (route decorators, `action="…"` literals, status literals, error-code literals) —
  the doc matches code, not memory.
- `version.py` == `pyproject.toml` == `1.1.0a17`.
