# v1.1.0a38 — purge all keosd / Antelope / EOSIO references

**Type:** docs ship (no daemon code change).
**Date:** 2026-05-31.
**Authoring:** Reviewer (Opus) at explicit operator direction ("purge all references to keosd (Antelope/EOSIO)").

## Why

The operator directed removal of every `keosd` / `Antelope` / `EOSIO` / `cleos`
reference. `fwd` had used `keosd` (the EOSIO/Antelope wallet daemon) as a
positioning analogy ("the keosd of Flare/EVM") and as the naming lineage for the
`clifwd` CLI. With the repo now public, the operator wants `fwd` described purely
on its own terms, with no third-party-IP association. References were fwd-only —
clif and the org-root constitution had none.

## What changed (13 references across 7 files → 0)

Current-state docs (reframed to describe `fwd` directly):
- `README.md` — the "What fwd is" intro no longer opens with the keosd analogy
  ("a policy-gated key-custody-and-signing daemon …"); the Provenance "Role" line
  reframed to "a custody daemon that holds keys and signs on behalf of clients".
- `CLAUDE.md` — tagline reframed; "Antelope" dropped from the not-supported-chains
  list; the "Carryover from FICSM and from keosd" section → "Carryover from FICSM"
  with the role described as the classic custody-daemon pattern (FICSM carryover
  preserved).

Append-only record — **surgical, operator-directed redaction**:
- `docs/history/0.2.3-clifwd-rename.md`, `docs/history/SHIP-LOG.md` — the `clifwd`
  naming rationale kept (`cl` + `i` + `fwd`, "Clifford") but the `cleos`/EOSIO
  lineage derivation removed.
- `docs/history/1.1.0a18-review-fixes.md`, `docs/history/1.1.0a22-current-state-docs-amendment.md`
  — "the keosd carryover" → "the carryover section".
- `docs/reviews/v1.0.0-system-independent-audit.md` — "keosd↔cleos boundary" →
  "fwd↔clif boundary"; dropped "the keosd lineage" from the scope list.

## Doctrine note (honest disclosure)

This ship edited the normally **append-only** record (Core invariant #13 / #18 —
`docs/history/` + `SHIP-LOG.md` + `docs/reviews/`). That was a deliberate,
operator-directed redaction of a specific third-party-IP reference — not
revisionism: every entry remains truthful about what shipped and when; only the
EOSIO/keosd association was removed, and each entry stays coherent. Done under
explicit operator instruction (operator authority is supreme over the doctrine).

## Verification
- `grep -rinE 'keosd|antelope|eosio|cleos'` across the whole fwd repo (excl.
  `.git`) → **0 matches**. clif + root constitution already clean.
- `pytest` version-consistency green (no `src/` change beyond the version bump).
