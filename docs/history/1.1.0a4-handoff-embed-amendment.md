# v1.1.0a4 — Constitutional amendment: hand-off embeds the file in the message

**Date:** 2026-05-19
**Reviewer:** Opus 4.7
**Implementer:** — (doctrine-only; the Reviewer authors the constitution
directly per § Development workflow — no Sonnet hand-off, no code change)
**Authorization:** operator, explicit, 2026-05-19 ("please wrap the file
inside the message. Always do so. Codify this in CLAUDE.md").

## What shipped

A constitutional-amendment ship (the second instance of the ship-type
D17 defines; the first was v1.1.0a3). It supersedes the prior
§ Development workflow wrapper rule — *"the wrapper points Sonnet at the
canonical prompt file rather than dumping the prompt contents into
chat"* — with: **the hand-off IS one self-contained markdown file whose
entire contents are the ready-to-paste message; the Reviewer hands over
only its path.**

- The Reviewer authors exactly one `~/.claude/plans/<name>.md`: a 2–4
  line framing preamble at the top (recipient; "the rest of this file is
  the authoritative spec"; operating boundary; a "Begin" line) + the
  full spec body. The whole file is the message.
- Delivered as the file path + "open it, select all, paste" — never
  assembled in or pasted out of the chat reply.
- Applies to EVERY hand-off — Sonnet implementer and peer/consumer Opus
  agents alike.

Reconciled honestly (Core #18, not a silent override): three forms were
tried 2026-05-19; the first two failed in practice — a pointer-only
message (fragile: assumed the file was where the recipient expected; the
clif FSP hand-off told the agent to read a file "in your working
directory") and the message dumped inline into the chat reply (forced
the operator to hand-extract it from Reviewer prose — explicitly
rejected). The prior anti-dump rationale is recorded as superseded
(neutralised by the standing self-contained-prompt requirement).

### Files (doctrine-only — bounded constitutional-amendment surface)

- `CLAUDE.md` — § Development workflow: the wrapper rule + its "Why this
  is enforced" rewritten; § Scope current line advanced a3 → a4 + the
  blocked clause re-sequences 9a-iii to v1.1.0a5 (Next paragraph
  untouched — per-ship single-line rule).
- `docs/decisions.md` — D18 (the recorded operator-directed act).
- `docs/history/README.md` — one index line.
- `pyproject.toml` + `src/fwd/version.py` — `1.1.0a4`.

Zero code change. Full suite stays 458 passed / 1 skipped; `ruff` /
`mypy` clean. v1.1.0a1/a2 FSP behaviour unaffected.

## Re-sequencing (Core #13 linear-forward; Core #18 honest history)

The bounded "9a-iii" reconciliation was re-sequenced a3 → v1.1.0a4 by
the v1.1.0a3 amendment; this ship takes v1.1.0a4, so it re-sequences
once more → **v1.1.0a5**, still hard-gated on GATE-1 live Coston2. The
plan is a plan; the linear-forward version invariant is the rule — same
discipline applied at v1.1.0a3.

## Scope note (surfaced, operator-gated)

Codified in fwd's CLAUDE.md only. Mirroring into the root
`proofs.africa/CLAUDE.md` "Sonnet hand-off demarcation (CRITICAL)"
section (cross-project binding) is an explicit operator decision —
surfaced in D18, not Reviewer-self-taken.

## Still gated (NOT shipped here)

- **9a-iii → v1.1.0a5** — the bounded post-verification reconciliation
  of Core #3/#7/#17 + CLAUDE.md:49. Hard-gated on GATE-1 live-Coston2.
- **GATE-1 live-Coston2** — env-deferred; the named Step-0 residual.
