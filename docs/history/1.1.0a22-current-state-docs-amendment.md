# v1.1.0a22 — Core #18 amended: current-state docs describe the present only

**Ship type:** constitutional amendment (operator-authorized; `decisions.md` D21) +
CLAUDE.md sanitization. No code change.

## Why

The current-state docs had accreted heavy archaeology — "Vault was retired at
v1.0.0a1", "was `/v1/sign-and-send`", "superseded", "vestigial", per-version "fixed in
vX" notes, and a ~1,250-word `CLAUDE.md` § Scope `a8 → a9 → … → a21` arrow-chain. The
prior Core #18 ("honest history") mandated keeping those notes in place; over many
ships they buried the present. Operator directive: the docs must describe the code as
it IS, not as it used to be.

## What changed (CLAUDE.md)

- **Core #18 amended** to "current-state docs describe the present only": present
  tense, no archaeology, no version narrative; when code changes the docs are
  rewritten (old wording replaced, not annotated-as-superseded). History lives ONLY in
  the quarantined record (`docs/history/` + `SHIP-LOG.md`, append-only Core #13; and
  `decisions.md`). Drift is still forbidden + caught by audit; the fix is rewrite-to-present.
- **§ Scope** collapsed from the a8→a21 arrow-chain to a clean current-state line.
- **Stripped the Vault / version / superseded / vestigial asides** from Core
  #1/#3/#4/#8/#11/#14/#16/#17/#19, the Identity section, the "What FWD IS NOT" list, the
  co-authorship + linear-forward process notes, and the carryover section. Each invariant
  keeps its rule; Core #17's DR-unverified and FSP-key risk flags are kept (present-tense).
- **Deleted the v0.3.0 → v2.x "Roadmap rollups"** (version history — it lives in
  `docs/history/` + `implementation-plan.md`).

After: `grep -ic` on CLAUDE.md for vault / sign-and-send / Scaleway / Shamir / AppRole /
Transit / vestigial / v1.0.0a1 → all 0. The only "retired/superseded" left are inside
Core #18 itself (the rule defining those words as forbidden) and the co-authorship
section stating the current root-rule.

## Scope (operator-chosen, D21)

Sanitize the **current-state docs + dead code/config**; **KEEP** the historical-record
files (`docs/history/`, `SHIP-LOG.md`, `decisions.md`, `implementation-plan.md`) as the
quarantined memory. This ship covers CLAUDE.md (the constitution); the four content
docs are a23, the dead code/config removal is a24.

## Doctrine surface (constitutional-amendment ship per D17)

CLAUDE.md (the amended Core #18 + the sanitized sections) + `decisions.md` D21 + this
history file + one `docs/history/README.md` index line + the two version files.

## Verification

CLAUDE.md archaeology grep → ~0 (only the #18 rule-definition). No code change; the
test suite is unaffected (a23/a24 carry their own verification).
