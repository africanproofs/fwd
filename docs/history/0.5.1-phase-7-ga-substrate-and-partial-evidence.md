# v0.5.1 — Phase 7 GA: drill-surfaced substrate wiring + deny-side live evidence (partial; approved/idempotency pending operator gas-funding)

**Shipped**: 2026-05-16
**Author**: Opus 4.7 Reviewer, driving the live drill. No Sonnet. The
v0.4.5/v0.4.6 precedent: the live verification drill surfaced real
doctrine↔substrate drift; this ship fixes it and records the evidence
proven so far. The gate is **partially met** — the entire deny side is
proven live; the broadcast-path vectors (V8/V9/V10 + idempotency)
remain pending an operator action (faucet-funding the gate wallet).
**Predecessor**: v0.5.0 (`a7b96d2` — Phase 7 GA runbook published;
committed local-only, push blocked by the GitLab account block).

## Why this ship

The operator ran `docs/runbooks/phase-7-verification.md` against live
Coston2 (ERC20_TOKEN `0xF6ca6bAEf426DfD38Aab69Fbad6CA3AfE4b6e29B`,
operator-supplied). Per Core invariant #14 the drill did exactly its
job: before any policy was evaluated it surfaced that **the deployed
stack could not run Phase 7 at all** — eight concrete drifts between
doctrine and the deployable artifact. v0.5.1 fixes the substrate,
re-runs the gate, and records what is proven.

## Eight drill-surfaced findings

| # | Finding | Resolution (v0.5.1) |
|---|---|---|
| 1 | Running `fwd:dev` image built 2026-05-13 — pre-Phase-7 (no policy engine / a6 / a7); the live container did not contain the code under test | Rebuilt `fwd:dev` from HEAD (`a7b96d2`) |
| 2 | Dockerfile never `COPY`d `config/` → ABI registry absent from image → `_startup_policy_load` fail-fast | **Dockerfile**: `COPY config/ /app/config/` |
| 3 | docker-compose.yml had no `FWD_POLICY_PATH`/`FWD_ABIS_DIR` and no policy.yaml mount → `load_policy` fail-fast | **docker-compose.yml**: `environment:` += both vars; `volumes:` += `./config/policy.yaml:/etc/fwd/policy.yaml:ro` (gitignored, Core inv #12) |
| 4 | `poetry.lock` content-hash stale since the a3 explicit `eth-abi` dep (image never rebuilt since) → `poetry install` aborts the build | `poetry lock --no-update` (content-hash only; zero dependency version change) |
| 5 | `sqlite3` absent in the slim runtime image → runbook's `docker exec fwd sqlite3` DB-inspection steps fail | Runbook amended to use `clifwd` / HTTP / `python -c` |
| 6 | Prior active drill callers (`phase6-drill-caller`, `phase5-gate-caller`) would trip D14 startup fail-fast against the minimal gate policy | Revoked via the old admin API pre-rebuild; runbook gains **Step 0.5 — revoke pre-existing active callers** |
| 7 | `clifwd` not on PATH (`poetry install --no-root` never creates the console script) — yet D16 promises `docker exec fwd clifwd audit verify` | **Dockerfile**: ship a `/usr/local/bin/clifwd` shim → the canonical command now resolves (validated: `chain intact: 11 rows`, exit 0) |
| 8 | Operator token `0xF6ca…e29B` is an EOA, not a contract on Coston2 | Accepted (operator-gated): the Phase 7 gate verifies *fwd* — decode→policy→sign→broadcast→custody — not token semantics. V10 evidence is scoped accordingly |

## What shipped (substrate)

- **Dockerfile**: `COPY config/ /app/config/` (ships the ABI registry —
  `policy.yaml` is NOT copied, it is operator-controlled + bind-mounted
  per Core invariant #12); a `clifwd` shim so D16's documented
  `docker exec fwd clifwd …` resolves.
- **docker-compose.yml** (fwd service): `environment:` += explicit
  `FWD_POLICY_PATH=/etc/fwd/policy.yaml`, `FWD_ABIS_DIR=/app/config/abis`
  (host-agnostic, Core inv #10); `volumes:` += the gitignored
  `./config/policy.yaml:/etc/fwd/policy.yaml:ro` mount.
- **poetry.lock**: content-hash reconciled (`poetry lock --no-update`;
  no version drift).
- Version 0.5.0 → 0.5.1 (PEP 440 linear-forward; Core inv #13).
- `docs/runbooks/phase-7-verification.md` amended for the deployable
  reality (Core inv #18): Phase-7-image + policy-mount prerequisites;
  Step 0.5 pre-revoke; the real `clifwd`/`python -c` invocation;
  `sqlite3`-free DB inspection; the EOA-target note.

Rebuilt + recreated only the `fwd` container (`--no-deps`; Vault stayed
unsealed). Vault was unsealed via the Core invariant #17 dev shortcut
file (`~/.fwd-dev/vault-init-output.json`, mode 0600, operator-owned —
the documented Phase 3a–7 procedure; disclosed). The new image booted
clean: `lifespan.policy_loaded` (callers=3, permissions=3, abis=[erc20,
participant_register, reward_manager]) and `lifespan.rate_buckets_pruned`
(the a7 `delete_stale` wiring, proven live at boot).

## Live evidence — DENY SIDE PROVEN (Coston2, fwd:dev rebuilt from a7b96d2)

Gate wallet `phase7-gate` = **`0xa1c51C9E76B0b10195EA81c0c940445Da55bb5a7`**
(public — Vault-custodied). Wallets `phase7-wr-wallet`
`0xa2b394978DfAE05f8b23d50C260B3dcd8A5d2b34`, `phase7-offlist`
`0x065821A3db5f718f62F4dC2b900B03ee3646a530`. Callers: `phase7-gate-caller`,
`phase7-cr-caller`, `phase7-wr-caller`.

**Step 1 + sub-checks — PASS:**
- 3 wallet-create + 3 caller-create via the a7 `AdminScope` + Vault
  path (all 201).
- D16 no-secret authorship: full dump of all audit rows searched for
  the THREE real generated api_keys + `fwd_live_` + `$argon2` +
  `api_key_hash` + `privkey` + `vault:v1:` → **all absent**; rows carry
  `caller=NULL`, `outcome` = `api_key_prefix`/`address` only.
- a7 admin policy_path validation: `POST /v1/admin/callers` with
  `policy_path: perm/nope` → **HTTP 400 `unknown_policy_path`**, and
  **no audit row** for the rejected-before-attempt create.
- `clifwd audit verify` → **chain intact**, exit 0.

**V1 — D14 step-1 caller→binding fail-fast — PASS:** an orphaned-caller
policy (`phase7-gate-caller` binding removed while it was an active DB
caller) → `lifespan.policy_consistency_error: caller
'phase7-gate-caller' policy_path 'perm/phase7-gate' not in policy` +
`SystemExit(1)`; the **D16 forensic `policy-load` `error` row (seq 8)
survived the exit** (the a6 commit-before-exit fix, proven live);
`clifwd audit verify` after restore → **chain intact: 11 rows**, exit 0.

**V2–V7 — D14 deny matrix — PASS** (all HTTP 403 `policy_denied`, exact
step; `GATE_ADDR` nonce stayed `0x0` throughout — proof NOT ONE
broadcast):

| Vector | Result |
|---|---|
| V2 contract not permitted | `403 step=2: contract not permitted for caller` |
| V3 decode fail (truncated) | `403 step=3: intent decode failed` |
| V3b unknown selector | `403 step=3: unknown selector for abi` |
| V4 method not in policy (`approve`) | `403 step=4: method signature not permitted` |
| V5 value > max | `403 step=5: max_value_wei exceeded` |
| V6 arg predicate (wrong recipient) | `403 step=6: arg 'to' predicate mismatch` |
| V7 wallet not allowlisted | `403 step=7: wallet not in caller allowlist` |

Default-deny is **real on the live signing path**: 7 of the 10 D14
steps + the step-1 startup fail-fast, each refusing with the correct
step, none reaching the chain.

## Pending — BROADCAST SIDE (operator action required)

V8 (step-8 caller rate), V9 (step-9 wallet rate), V10 (step-10
approved + mined), and the idempotency replay all require the gate
wallet to hold C2FLR for gas. Funding is a manual operator action
(faucet) — there is no programmatic funding path. **Required:** fund
`0xa1c51C9E76B0b10195EA81c0c940445Da55bb5a7` with C2FLR
(https://faucet.flare.network/coston2), then the drill resumes:
V8/V9 first-call broadcasts + second-call step-8/9 deny; V10
`transfer`-shaped call → mines `status=0x1`, on-chain `from` =
`0xa1c5…b5a7` (secp256k1 custody recovery — the strong Core inv #14
proof; target is an EOA so no token-state assertion, per finding 8);
idempotency replay → same `tx_id`, no second broadcast,
`sign-and-send-duplicate` row; final `clifwd audit verify`.

The Phase 7 GA gate is **partially met**. The verification-met
addendum recording the broadcast-side on-chain evidence is a
Reviewer-only follow-up once the operator funds the wallet and the
remaining vectors are run.

## Gates (substrate ship — code unchanged)

- 414 passed + 4 skipped; ruff/format/mypy(51)/layer/version green —
  no `src/` change beyond `version.py`. Version anchors 0.5.0 → 0.5.1.
- `poetry.lock` reconciled (content-hash only). `docker compose build
  fwd` succeeds; the rebuilt image boots Phase-7-clean.

## Commit

Single terse conventional-commit line, NO co-author trailer, operator
sole author (root constitution NON-NEGOTIABLE rules, 2026-05-16).
Committed local-only; push remains blocked by the GitLab account block
on `@makhosonke`; the a3→v0.5.1 stack is locally unpushed,
`origin/main` at `f1ee3f1`.
