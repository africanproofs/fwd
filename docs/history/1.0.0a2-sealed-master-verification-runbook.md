# v1.0.0a2 — sealed-master verification runbook published (Reviewer-only)

**Shipped**: 2026-05-17
**Author**: Opus 4.7 Reviewer. No Sonnet; no code/test change.
**Predecessor**: v1.0.0a1 (`5984eb1`, Vault → sealed master).

Published `docs/runbooks/v1.0.0a1-sealed-master-verification.md` — the
operator-run Core invariant #14 live gate for the v1.0.0a1 custody
swap. It proves the sealed-master path end-to-end: a real Coston2 tx
via `POST /v1/sign-and-send` mines on-chain and the on-chain `from`
(secp256k1-recovered) equals the address `fwd` AES-256-GCM-sealed under
the mode-0600 host-file master — impossible unless `SealedMaster`
decrypted the exact privkey from its `seal:v1:` ciphertext, with no
Vault anywhere in the path.

Same precedent as v0.3.0 (`sign-and-send-verification.md`) and v0.5.0
(`phase-7-verification.md`): the runbook is published now; the
**verification-met addendum** (on-chain evidence: wallet, tx hash,
block, `from` match, `clifwd audit verify` rows) is a Reviewer-only
follow-up appended to `docs/history/1.0.0a1-vault-to-sealed-master.md`
once the operator runs it. That clean pass gates **Phase 8b** (the
`ftso-fee-claimer` cutover and the first deleted `.env PRIVATE_KEY=`
line — v1.0.0, which lifts the doctrine-ship freeze).

The runbook carries the binding amendments learned from prior live
drills (Core #14): Phase-correct image; policy mounted; Step 0 master
key 32-byte/0600/uid-1000 with the bind-mount ownership gotcha called
out; Step 0.6 revoke stale callers; `clifwd` invocation; no `sqlite3`
in the slim image; `/healthz` field is `master` not `vault`.

No code/test change; 426 passed + 0 skipped, gates green; version
1.0.0a1 → 1.0.0a2. Single terse conventional-commit line, NO trailer,
operator sole author. Committed local-only; push remains blocked by the
GitLab account block; `origin/main` at `f1ee3f1`.
