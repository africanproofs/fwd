# v1.1.0a23 — content docs sanitized to present-tense

**Ship type:** documentation sanitization (per Core #18 as amended in a22 / D21). No
code change.

## Why

a22 amended Core #18 (current-state docs describe the present only) and sanitized
CLAUDE.md. This ship applies the same rule to the four remaining current-state docs,
which were riddled with archaeology — Vault, the retired `sign-and-send` endpoint,
Scaleway, per-version "shipped/fixed in vX" provenance, and version-by-version notes.

## What changed

- **README.md** — versionless status line; § Custody model trimmed to present-tense
  (no "replaced HashiCorp Vault at v1.0.0a1" narrative); doc-table + Provenance asides
  stripped; runbooks entry simplified.
- **docs/architecture.md** — full present-tense rewrite (Sonnet-implemented in an
  isolated worktree, Reviewer binding-pass + transferred to main). Removed all Vault /
  Scaleway / `sign-and-send` references and every "v0.X / v1.x.aN / Shipped vX /
  fixed vX / corrected vX" provenance from the summary, topology, signing-flow, schema
  comments, API table, master-key + credential-lifecycle sections, backup/restore,
  policy engine, intent decoder, audit-log, layer boundaries, and implementation
  hazards. Kept all current technical content + the deferred-phase markers + the
  present-tense risk flags. Live code symbols `vault_master_key` (column) and
  `vault_unavailable` (error code) keep their names, described plainly.
- **docs/threat-model.md** — deleted the retired-Vault attack analyses A5–A8 and
  renumbered A9–A12 → A5–A8; deleted attacker tiers 6–7 (GPG/Shamir) and renumbered;
  removed the A4 "zero-egress update" banner (folded into the present-tense body);
  dropped the struck AppRole summary-table row and the v0.X/Vault asides throughout.
  The live attack surface (caller / host-root / process / physical / supply-chain /
  side-channel / crypto-break) + the present-tense risk flags remain.
- **docs/dependencies.md** — deleted the struck `~~hvac~~` / `~~prometheus-client~~` /
  `~~tenacity~~` rows; removed the Vault/version asides + the historical "Verification
  spike (Phase 1)" section; present-tense throughout.

After: `grep` across the four docs for the archaeology vocabulary → clean (only the
live `vault_master_key` / `vault_unavailable` code symbols + present-tense "no unseal
step" remain). The history of all this lives in `docs/history/`, `decisions.md`, and
`implementation-plan.md` (kept, quarantined — Option A / D21).

## Doctrine surface

The four content docs + this history file + one `docs/history/README.md` index line +
the two version files. No code change; the architecture.md rewrite was verified by
grep + Reviewer binding pass (no test impact — docs only).

## Not closed here

Dead code/config removal (the retired Vault-snapshot sidecar + scripts, the stale
Vault runbooks, the vestigial `replaced` status) is a24.
